# تقرير الأجزاء غير المطابقة بالكامل

## أجيال المعرفة — تطبيق الطالب والمعلم

**تاريخ التقرير:** 2026-09-05  
**النطاق:** المخططات الهندسية الأربعة، API server، تطبيق Expo، التخزين، الدفع، الجلسات، الأدوار، المهام، الرسائل والمكالمات.  
**الغرض:** حصر كل جزء لا يمكن اعتباره مطابقاً بالكامل للمخططات، مع التفريق بين المخالفة التي يثبتها الكود وبين النقطة التي تحتاج إثباتاً من Supabase Production.

## 1. طريقة قراءة التقرير

### مستويات الحالة

- **مخالفة مؤكدة:** الكود الحالي ينفذ سلوكاً يختلف مباشرة عن المتطلب الهندسي.
- **مطابقة جزئية:** جزء من التدفق موجود، لكن دورة العمل أو الحماية أو الحالة النهائية غير مكتملة.
- **غير مثبت:** قد يكون Production يحقق السلوك عبر Trigger/RPC/Edge Function، لكن ذلك غير ظاهر في الكود ولم يمكن قراءته من اتصال Supabase الحالي.
- **غير قابل للإصلاح من الهاتف وحده:** يحتاج إلى عقد خادمي موجود مثبت أو تغيير في Production، وهو غير مسموح حالياً دون موافقة صريحة.

### مستويات الخطورة

- **P0:** قد يسبب قبولاً مكرراً، خصماً مالياً خاطئاً، فقدان حجز، تجاوز صلاحيات، أو عدم اتساق بين الحجز والجلسة.
- **P1:** دورة رئيسية ناقصة أو حماية/تدقيق غير مكتمل، لكن الخطر ليس خصماً فورياً في كل مسار.
- **P2:** نقص في العرض، التوافق، التوثيق أو تجربة الاستخدام لا يغيّر الحالة المالية مباشرة.

## 2. ملخص المخاطر

| الرقم | الجزء | الحالة | الخطورة |
|---:|---|---|---|
| 1 | قبول الحجز وإنشاء الحجز والجلسة | مخالفة/غير مثبت | P0 |
| 2 | فحص الرصيد والتعارض قبل القبول | غير ذري | P0 |
| 3 | تسوية الجلسة والخصم والأرباح | غير مثبت | P0 |
| 4 | هوية الدور واعتماد المعلم | مطابقة جزئية | P0/P1 |
| 5 | الإلغاء وإرجاع الرصيد | مسار مباشر غير مثبت | P1 |
| 6 | حضور الجلسة والـ heartbeat | مطابقة جزئية | P1، وترتفع مالياً إلى P0 |
| 7 | تسليم الطالب للواجب | مخالفة مؤكدة | P1 |
| 8 | مراجعة المعلم واعتماد الدرجة | قراءة فقط | P1 |
| 9 | المواد والتسجيلات | قراءة جزئية | P1 |
| 10 | طلبات السحب وتاريخ الحالات | مطابقة جزئية | P1 |
| 11 | محفظة الاتصال والشحن | غير مثبت مالياً | P0/P1 |
| 12 | الرسائل والمرفقات | مطابقة جزئية وتحتاج Production | P1 |
| 13 | المكالمات الداخلية والإشعارات | حماية وتحقق ناقصان | P1 |
| 14 | OAuth وDeep Links | كود موجود، Production غير مثبت | P1 |
| 15 | Realtime وStorage policies | غير مثبتة | P0/P1 |

---

## 3. الحجوزات والقبول

### 3.1 قبول الحجز ليس معاملة ذرية كاملة — P0

**المتطلب الهندسي**

المخطط الرئيسي يصف قبول الحجز كعملية واحدة تشمل:

1. قفل طلب الحجز.
2. قفل الاشتراك والوقت.
3. فحص الرصيد والتعارض.
4. تحديث الطلب إلى accepted.
5. إنشاء `booking`.
6. إنشاء `session`.
7. إنشاء الإشعارات.
8. تنفيذ commit واحد.

المراجع:

- `platform-master-integrated-architecture_1788629894258.md:843-861`
- `platform-master-integrated-architecture_1788629894258.md:991-1000`
- `teacher-dashboard-architecture_1788629894486.md:181-191`

**السلوك الحالي**

في `artifacts/api-server/src/routes/learning.ts`:

- قراءة الطلب والفحوص الأولية: `:544-613`.
- استدعاء `accept_booking_request`: `:632-640`.
- استدعاء `accept_booking_group`: `:614-630`.
- إعادة قراءة الاشتراك والحجوزات: `:647-670`.
- إنشاء `bookings` يدوياً: `:672-688`.
- إرسال الإشعار ورسالة الدردشة لاحقاً: `:694-731`.

**سبب عدم المطابقة**

هناك عدة طلبات منفصلة بين الفحص والقبول والإنشاء. الكود لا يثبت أن RPC القبول:

- يقفل الصفوف.
- ينشئ `bookings`.
- ينشئ `sessions`.
- يتعامل مع إعادة المحاولة.
- يمنع قبولين متزامنين.

في حالة نجاح RPC ثم فشل POST الخاص بـ`bookings` يمكن أن يصبح الطلب accepted دون جلسة فعلية. وفي حالة التنافس يمكن أن ينجح فحصان قبل أن يكتب أي منهما.

**الحكم:** مخالفة/غير مثبت — **P0**.

**الإصلاح الآمن**

- لا يمكن ضمان الذرية من React أو من API عبر فحوص إضافية.
- يجب أولاً إثبات side effects وsignature لـ`accept_booking_request` و`accept_booking_group`.
- إذا كان RPC ينشئ الحجز والجلسة، يزال الإنشاء اليدوي من API ويصبح read-after-write للتحقق فقط.
- إذا لم يكن RPC ينشئهما، يلزم استخدام عقد خادمي ذري موجود أو اعتماد تغيير في Production.

---

### 3.2 فحص الرصيد ليس القرار النهائي — P0

**المتطلب**

القرار النهائي للرصيد يجب أن يحدث داخل مسار خادمي مقفول، مع مراعاة مدة الحجز كاملة والمجموعة والتعارضات.

**السلوك الحالي**

في `learning.ts:588-599`:

- API يقرأ `user_subscriptions`.
- يشترط `is_active = true`.
- يشترط `remaining_minutes >= 15`.

ثم بعد استدعاء RPC يعيد القراءة في `:647-658`.

**الفجوة**

- القراءة الأولى ليست lock.
- قد يتغير الرصيد بين القراءة والقبول.
- شرط 15 دقيقة لا يثبت كفاية رصيد كل عناصر المجموعة.
- إعادة القراءة بعد القبول لا تعيد العملية إلى نقطة ذرية.
- لا يوجد في API خصم أو reserve مالي، وهذا صحيح فقط إذا كانت المنصة الأصلية تفعل ذلك داخل Trigger/RPC، وهو غير مثبت.

**الحكم:** مطابقة جزئية/غير ذري — **P0**.

**الإصلاح الآمن**

يُبقى الفحص كتحسين للرسالة فقط، ولا يُعامل كقرار أمني أو مالي. القرار يجب أن يأتي من RPC/Trigger Production مثبت.

---

### 3.3 إنشاء `session` بعد القبول غير مثبت — P0

**المتطلب**

بعد قبول الطلب يجب أن يوجد `booking` و`session` مرتبطة به، مع حالات الجلسة المطلوبة.

**السلوك الحالي**

مسار القبول في `learning.ts:672-688` ينشئ صفوف `bookings` فقط. لا يوجد إنشاء صريح لـ`sessions` في هذا المسار.

**الفجوة**

التطبيق يعتمد ضمنياً على Trigger Production غير مقروء. لا يمكن من الكود وحده إثبات أن:

- كل booking ينتج session.
- session لها `booking_id` unique.
- failure في Trigger يعيد العملية كلها.
- session تبدأ بالحالة الصحيحة.

**الحكم:** غير مثبت — **P0**.

**الإصلاح الآمن**

إضافة فحص read-after-write يمكن أن يكشف الخلل فقط، لكنه لا يصلحه. لا يجوز إنشاء `sessions` من التطبيق كحل بديل.

---

### 3.4 إنشاء طلب الحجز نفسه متعدد الخطوات — P1

**المتطلب**

إنشاء طلب الطالب يجب أن يتحقق من الملف، الاشتراك، المعلم المعتمد، المادة، المرحلة، التكرار والموعد، ثم ينشئ `booking_request` ويشعر المعلم.

**السلوك الحالي**

في `learning.ts:385-528`:

- الفحوص كلها تتم قبل POST.
- التكرار يُفحص في `:461-487`.
- الطلب يُدرج في `:489-503`.
- الإشعار يُرسل بعد الإدراج في `:509-525`.

**الفجوة**

توجد نافذة بين فحص التكرار وPOST. قد ينشئ طلبان متزامنان نفس الحجز إذا لم يوجد unique constraint أو RPC خادمي. كما أن فشل الإشعار بعد الإدراج متوقع ومقبول، لكن لا يوجد دليل أن Trigger Production يتولى الإشعار أيضاً.

**الحكم:** مطابقة جزئية — **P1**.

---

## 4. الجلسات والفوترة

### 4.1 إنهاء الجلسة والخصم والأرباح غير مثبتة — P0

**المتطلب**

المخطط المالي يطلب أن ينتج إنهاء الجلسة عن:

- حساب المدة الفعلية.
- تحديد الجلسة القصيرة.
- خصم الدقائق من الاشتراك.
- تحديث حالة الحجز.
- إنشاء/تحديث `teacher_earnings`.
- تحديث سجل الجلسة.
- منع الخصم المكرر.
- إنشاء مواد أو تقرير الجلسة عند الحاجة.

المراجع:

- `full-session-booking-finance-assignments-architecture_1788629894391.md:908-933`
- `full-session-booking-finance-assignments-architecture_1788629894391.md:950-979`
- `platform-master-integrated-architecture_1788629894258.md:1003-1008`

**السلوك الحالي**

في `app/live-session.tsx:112-127` ينهي التطبيق WebRTC عبر `endCall()`.  
ويقرأ حالة `sessions` و`bookings` في `:33-46` و`:91-110`.

في `ConnectedSectionScreen.tsx:96-107` و`:302-319` تتم قراءة:

- `deducted_minutes`
- `short_session`
- `started_at`
- `ended_at`

لكن لا يوجد في الهاتف مسار authoritative لتسوية المال.

**الفجوة**

لا يمكن إثبات من الكود أن:

- إعادة الضغط على الإنهاء لا تخصم مرتين.
- الجلسة القصيرة لا تخصم ولا تنشئ أرباحاً بالخطأ.
- خصم الطالب وربح المعلم يتمان في معاملة واحدة.
- `booking.status` و`session.session_status` ينتقلان معاً.
- الفشل الجزئي يعاد بأمان.

**الحكم:** غير مثبت — **P0**.

**الإصلاح الآمن**

لا تضع أي حساب مالي في الهاتف. يجب إثبات Trigger/RPC الأصلي وقيود idempotency في Production، ثم يقتصر الهاتف على القراءة وإظهار الحالة.

---

### 4.2 حضور الجلسة والـ heartbeat غير مكتملين في التطبيق — P1/P0 مالي

**المتطلب**

المخططات تشترط شروطاً مثل:

- بدء الاجتماع فعلياً.
- انضمام الطرفين.
- اتصال صحي.
- `active_sessions`.
- heartbeat.
- `session_events`.
- مصدر خادمي لوقت البدء والانتهاء.

المراجع:

- `full-session-booking-finance-assignments-architecture_1788629894391.md:694-747`
- `platform-master-integrated-architecture_1788629894258.md:1001-1006`

**السلوك الحالي**

في `live-session.tsx:47-57` يسمح التطبيق للطالب بالانضمام بناءً على:

- وجود `started_at`.
- `bookings.session_status = in_progress`.

ويوجد WebRTC في `useSessionWebRTC.ts`.

لكن لا يظهر في مسار الهاتف تسجيل واضح لـ:

- `active_sessions`.
- heartbeat.
- `session_events`.
- شرط أن الطرفين اتصلا قبل اعتبار الحصة بدأت مالياً.

**الفجوة**

وجود اتصال WebRTC لا يثبت دورة الحضور الخادمية ولا يثبت أن الوقت القابل للفوترة يبدأ وينتهي في النقطة الصحيحة.

**الحكم:** مطابقة جزئية — **P1**، وترتفع إلى **P0** إذا كان هذا التوقيت يدخل في الفوترة.

---

### 4.3 الطالب لا يبدأ دورة المعلم من الهاتف — مطابق جزئياً

منع بدء المعلم للجلسة من الهاتف متوافق مع قرار أن الهاتف عميل انضمام للطالب، لكن هذا لا يثبت أن:

- واجهة الكمبيوتر هي التي تكتب الحالات الصحيحة.
- الهاتف يمنع كل مسارات البدء غير المباشرة.
- Production يملك Trigger متسقاً مع هذا القرار.

المراجع:

- `live-session.tsx:136-137,161-165`
- `.agents/memory/mobile-session-device-policy.md`

**الحكم:** القرار المعماري متوافق، لكن إثبات دورة Production غير مكتمل — **P1**.

---

### 4.4 إلغاء الحجز يكتب مباشرة إلى `bookings` — P1

**المتطلب**

انتقالات الحالات يجب أن تكون منضبطة، مع عدم إعادة الدقائق أو إنشاء refund غير موجود في المنصة. الحالات العكسية مثل `completed -> confirmed` يجب رفضها.

المراجع:

- `full-session-booking-finance-assignments-architecture_1788629894391.md:599-625`
- `teacher-dashboard-architecture_1788629894486.md:039-042`

**السلوك الحالي**

في `learning.ts:1041-1084`:

- قراءة booking.
- PATCH مباشر إلى `bookings`.
- تعديل `status` و`session_status`.
- تحديث حقول الإلغاء.

ثم ترسل الإشعارات في `:1097-1117`.

**الفجوة**

- القراءة والكتابة ليستا انتقالاً ذرياً.
- لا يوجد في المسار إثبات RPC إلغاء أصلي.
- لا يوجد refund أو reserve مثبت.
- إعادة الطلب أثناء التنافس قد تتصرف بشكل مختلف حسب RLS/Trigger غير المقروء.

**الحكم:** مخالفة/غير مثبت — **P1**.

**الإصلاح الآمن**

استخدام RPC/Edge Function الأصلي بعد إثباته، وعدم إعادة الدقائق أو إنشاء refund من العميل.

---

## 5. الأدوار والصلاحيات

### 5.1 حارس الدور واعتماد المعلم غير موحد — P0/P1

**المتطلب**

يجب أن تعتمد المسارات على:

- هوية Supabase.
- `user_roles` أو claims الموثوقة.
- حظر الحساب.
- `teacher_profiles.is_approved`.
- RLS وRPC كطبقة نهائية.

المراجع:

- `full-session-booking-finance-assignments-architecture_1788629894391.md:131-145,185-195`
- `teacher-dashboard-architecture_1788629894486.md:009-015,041-042,146-155`

**السلوك الحالي**

الهاتف لديه حارس انتظار الدور والاعتماد في:

- `app/_layout.tsx:49-56,290-313`.

والـAPI يفحص role في:

- `learning.ts:340-356`
- `learning.ts:398-440`
- `learning.ts:540-542`
- `learning.ts:837-849`.

لكن بعض المسارات تعتمد على role دون guard مركزي لـ`is_approved`، مثل:

- `learning.ts:946-952`
- `learning.ts:1058-1062`.

كما أن `supabaseAuth.ts:73-110` ينفذ قراءات REST منفصلة بدلاً من عقد خادمي موحد يثبت الدور والاعتماد والحظر معاً.

**الفجوة**

الهاتف ليس مصدراً نهائياً للصلاحيات. وقد يصل معلم يملك role صحيحاً إلى مسار لا يفرض الاعتماد إذا لم تمنعه RLS/Production.

**الحكم:** مطابقة جزئية — **P0** للصلاحيات الحساسة و**P1** لبقية المسارات.

**الإصلاح الآمن**

توحيد guard في API، مع إبقاء RLS مصدر القرار النهائي. لا يتم fallback إلى student عند عدم حسم الدور.

---

### 5.2 إثبات RLS والسياسات غير متاح — P0/P1

لا يمكن اعتبار الحماية مكتملة دون إثبات سياسات:

- `profiles`
- `teacher_profiles`
- `public_teacher_profiles`
- `booking_requests`
- `bookings`
- `sessions`
- `assignment_submissions`
- `wallets`
- `teacher_earnings`
- `withdrawal_requests`
- `chat_messages`
- `internal_calls`

اتصال Supabase الحالي لم يوفر OpenAPI أو metadata موثوقة، وبعض الطلبات أعادت `403/429`. لذلك هذه فجوة إثبات وليست حكماً بأن RLS مفقودة.

---

## 6. الواجبات والاختبارات

### 6.1 الطالب لا ينفذ تسليم الواجب فعلياً — مخالفة P1

**المتطلب**

يجب أن يرى الطالب:

- أسئلة الواجب.
- الإجابات النصية والاختيارية.
- الصور/PDF/الصوت حسب إعداد الواجب.
- زر إرسال ينشئ `assignment_submissions`.
- استدعاء `grade-assignment`.

المراجع:

- `full-session-booking-finance-assignments-architecture_1788629894391.md:1437-1440,1480-1510`
- `student-dashboard-architecture_1788629894440.md:1060-1100`

**السلوك الحالي**

في `app/(tabs)/assignments.tsx:55-83`:

- يظهر تنبيه للطالب.
- يستدعي `completeAssignment(id)`.
- يطلب من الطالب إرسال الإجابات من مسار آخر في المنصة.

وفي `learning.ts:1173-1290`:

- يتم التحقق من وجود `assignment_submissions`.
- لا يتم إنشاء submission.
- `complete` ليس بديلاً عن الإجابة.

**الفجوة**

الواجهة لا تنفذ دورة الطالب الكاملة. زر «تحقق من التسليم» قد يعطي انطباعاً بأن المهمة اكتملت دون أن يكون الهاتف قد أنشأ الإجابات أو رفع المرفقات.

**الحكم:** مخالفة مؤكدة — **P1**.

**الإصلاح الآمن**

ربط الواجهة بعقد submission الأصلي بعد إثبات:

- أسماء الحقول.
- bucket.
- Storage policies.
- Edge Function grading.

ولا يجوز تغيير معنى `completeAssignment` ليصبح submission تخمينياً.

---

### 6.2 مراجعة المعلم قراءة فقط — P1

**المتطلب**

يجب أن يستطيع المعلم:

- قراءة submission.
- تشغيل AI grading عند الحاجة.
- كتابة `teacher_score`.
- كتابة `teacher_feedback`.
- اعتماد `final_score`.
- تغيير حالة submission إلى `reviewed`.
- إشعار الطالب.

المراجع:

- `teacher-dashboard-architecture_1788629894486.md:427-430,459-463,587`
- `full-session-booking-finance-assignments-architecture_1788629894391.md:1517-1594`

**السلوك الحالي**

في `assignments.tsx:35-53` تتم قراءة `assignment_submissions` فقط.  
وفي `:111-136` تظهر:

- الحالة.
- الطالب.
- وقت التسليم.
- الدرجة الموجودة إن وجدت.
- الملاحظات الموجودة إن وجدت.

لا يوجد:

- input للدرجة.
- input للملاحظات.
- invoke لـ`grade-assignment`.
- update/RPC للاعتماد.
- إشعار للطالب.

**الحكم:** مخالفة مؤكدة — **P1**.

**الإصلاح الآمن**

لا تُكتب الحقول مباشرة من الهاتف قبل إثبات RPC/Edge Function وسياسة الصلاحيات. يجب استعمال عقد المراجعة الأصلي إن كان موجوداً.

---

### 6.3 AI grading غير موصول — P1

المخطط يتطلب:

- `submitted -> ai_graded`.
- `ai_score`.
- `ai_feedback`.
- `ai_breakdown`.
- مراجعة المعلم.
- `reviewed`.

لا يوجد invoke لـ`grade-assignment` في التطبيق الحالي. لذلك دورة التصحيح الآلي غير موجودة في الهاتف، حتى لو كانت Edge Function موجودة في Production.

**الحكم:** غير موجود في التطبيق/غير مثبت في Production — **P1**.

---

## 7. المواد والتسجيلات

### 7.1 المواد قراءة جزئية بلا CRUD للمعلم — P1

**المتطلب**

`session_materials` يجب أن تدعم:

- مواد الجلسة.
- الملفات.
- السبورة.
- التسجيل.
- العرض للطالب والمعلم.
- إنشاء/تعديل/حذف وفق صلاحية المعلم.
- `is_deleted`.
- انتهاء/احتفاظ الملفات.

المراجع:

- `teacher-dashboard-architecture_1788629894486.md:314,515-516,540,606`
- `full-session-booking-finance-assignments-architecture_1788629894391.md:788-886`

**السلوك الحالي**

ملفا:

- `app/materials.tsx`
- `app/teacher-materials.tsx`

هما wrappers، بينما التنفيذ في `ConnectedSectionScreen.tsx:632-644` يقرأ المواد فقط مع:

- `is_deleted = false`.
- `expires_at`.

ويفتح التسجيل عبر Signed URL في `:616-638`.

لا يوجد في الهاتف:

- create.
- update.
- delete.
- upload metadata.
- `save-session-recording`.
- إدارة دورة الاحتفاظ.

**الحكم:** مطابقة جزئية — **P1**.

---

## 8. السحب والأرباح ومحفظة الاتصال

### 8.1 طلب السحب غير ذري — P1

**المتطلب**

طلب السحب يجب أن يضمن:

- فصل أرباح التدريس عن محفظة الاتصال.
- حجز/تثبيت المبلغ المتاح.
- منع طلبين متزامنين لنفس الرصيد.
- إشعار الإدارة.
- سجل تدقيق.
- انتقالات حالة خادمية.

**السلوك الحالي**

في `TeacherFinanceScreens.tsx:133-168`:

1. يقرأ `available`.
2. يرفع الملف.
3. يدرج `withdrawal_requests`.
4. يرسل إشعاراً إلى المعلم نفسه في `:169-174`.

**الفجوة**

- طلبان متزامنان قد يرسلان نفس `available`.
- فشل الإدراج بعد رفع الملف قد يترك ملفاً يتيماً.
- لا يظهر إشعار الإدارة.
- لا توجد عملية reserve خادمية مثبتة.

**الحكم:** مطابقة جزئية — **P1**، مع أثر مالي محتمل.

---

### 8.2 تاريخ حالات السحب مفقود — P1

المخطط يطلب `withdrawal_status_history` لتسجيل:

- الحالة السابقة.
- الحالة الجديدة.
- من غيّرها.
- وقت التغيير.

الكود في `TeacherFinanceScreens.tsx:81-107` يقرأ `withdrawal_requests` فقط، وفي `:217-225` يعرض status الحالي.

كما أن `WithdrawalStatus` في `:52-62` يحوّل أي قيمة غير معروفة إلى `pending`.

**المشكلة**

هذا fallback قد يعرض حالة مالية غير صحيحة للمستخدم إذا أضاف Production حالة جديدة مثل `under_review`, `processing` أو `failed`.

**الحكم:** مخالفة جزئية/غير مثبت — **P1**.

---

### 8.3 محفظة الاتصال وسعر الدقيقة غير مكتملين — P0/P1

**المتطلب**

محفظة الاتصال منفصلة عن أرباح المعلم، ويجب أن يكون:

- الشحن عبر Stripe/webhook.
- الخصم عبر دالة خادمية.
- سجل الحركات ذرياً.
- سعر الدقيقة من إعداد خادمي.

**السلوك الحالي**

في `TeacherFinanceScreens.tsx`:

- قراءة `wallets` و`call_logs`: `:245-260`.
- تحقق الشحن بعد العودة: `:264-274`.
- بدء checkout: `:276-291`.
- سعر ثابت في الواجهة: `:308`، بقيمة `0.30`.

**الفجوة**

- لا يثبت الكود أن العودة من Stripe تعني الدفع المؤكد.
- لا يثبت idempotency للـcredit.
- سعر الدقيقة ليس قراءة من إعداد خادمي.
- لا يظهر مسار wallet transaction في هذا المكون.

**الحكم:** غير مثبت مالياً — **P0** للـcredit و**P1** للسعر/العرض.

**الإصلاح الآمن**

الـwebhook/Edge Function فقط هو مصدر credit. الهاتف يعرض نتيجة الخادم ولا يحسب أو يضيف رصيداً محلياً.

---

## 9. الرسائل والمرفقات

### 9.1 مسار المرفقات متوافق جزئياً — P1

**المتطلب**

المرفقات يجب أن تكون:

- داخل bucket خاص.
- مرتبطة بالحجز.
- في مسار لا يسمح بالعبور بين الحجوزات.
- مفتوحة عبر Signed URL.
- محمية بـStorage policies.

**السلوك الحالي**

في `MessagesScreen.tsx`:

- الرفع إلى `chat-files/<bookingId>/...`: `:136-158`.
- حفظ path.
- Signed URL: `:61-83,215-241`.
- Realtime وmark-read: `:358-394,658-669`.

وهذا متوافق جزئياً مع المخطط.

**الفجوة**

- Storage policy الفعلية غير مثبتة.
- Realtime publication غير مثبتة.
- العزل يعتمد على RLS غير المقروءة.
- لم يُختبر حساب طالب ومعلم حقيقيان مع PDF وصورة.

**الحكم:** مطابقة جزئية — **P1**.

---

### 9.2 إشعارات الرسائل ليست معاملة واحدة — P1

إدراج الرسالة ثم إرسال push عبر endpoint منفصل في `MessagesScreen.tsx:376-393` يعني أن:

- الرسالة قد تصل دون push.
- push قد يفشل بعد نجاح الرسالة.
- لا يوجد rollback مطلوب للرسالة، لكن يجب أن يكون ذلك موثقاً كسلوك best-effort.

إذا كانت الوثيقة تشترط إشعاراً مضموناً، يلزم Trigger/Edge Function. أما إذا كان push best-effort، فيجب توثيق ذلك وعدم اعتباره فشلاً في الرسالة نفسها.

---

## 10. الشهادات والمرفقات الخاصة

### 10.1 مسار الشهادات الجديد تم إصلاحه، لكن Production غير مثبت — P1

تم تعديل `app/(tabs)/profile.tsx` ليحفظ path داخل `support-files` ويستخدم Signed URL.

هذا يطابق شرط الخصوصية للسجلات الجديدة، لكن لا يمكن إغلاق البند بالكامل لأن:

- bucket privacy غير مثبتة.
- Storage policy غير مقروءة.
- الروابط التاريخية العامة ما زالت مدعومة للتوافق.
- لا يوجد تحقق خادمي مستقل للحجم والنوع إن كان العقد الأصلي يتطلبه.

**الحكم:** مطابق للكود الجديد جزئياً، يحتاج تحقق Production — **P1**.

---

## 11. OAuth وDeep Links

### 11.1 كود OAuth موجود، لكن إعداد Production ومسار redirect غير مثبتين — P1

**المتطلب**

المخطط يتطلب:

- حفظ الدور المطلوب قبل OAuth.
- استكمال الدور بعد العودة.
- الحفاظ على redirect داخلي آمن.
- دعم web وnative وcold start.

**السلوك الحالي**

- Google و`pending_role` في `lib/auth.tsx:66-84,176-226`.
- callback route في `app/_layout.tsx:121-129,333-335`.
- إكمال الدور في `app/auth/callback.tsx:7-26`.

**الفجوة**

- لا يمكن إثبات Allowed Redirect URLs في Supabase/Google.
- لا يوجد إثبات اختبار Production native/web.
- callback يعيد المسار الافتراضي في بعض الحالات، بينما الوثيقة تتوقع الحفاظ على redirect داخلي.

**الحكم:** مطابقة جزئية — **P1**.

**الإصلاح الآمن**

اختبار Production على:

- فتح بارد.
- إغلاق التطبيق أثناء OAuth.
- web.
- native.
- redirect إلى مسار داخلي.

ولا يتم تعديل إعدادات OAuth من التطبيق.

---

## 12. المكالمات الداخلية والإشعارات

### 12.1 التحقق من طرف المكالمة غير كافٍ — P1

**المتطلب**

يجب أن لا يستطيع المستخدم قبول أو إنهاء مكالمة إلا إذا كان طرفاً مصرحاً بها، وأن ترتبط المكالمة بالمستخدم/الحجز الصحيح.

**السلوك الحالي**

في `api-server/src/routes/push.ts`:

- registration/deletion للـtokens: `:36-77`.
- رسائل booking-scoped: `:80-128`.
- incoming push: `:131-199`.
- قبول/إنهاء المكالمة: `:201-295`.

الفحص الظاهر يتحقق من أن المستلم ليس caller، لكنه لا يثبت في التقرير الحالي تحققاً كاملاً من:

- أن `callId` يعود للمستخدم.
- أن المستخدم participant في `internal_calls`.
- أن المكالمة مرتبطة بحجز أو طرف صحيح.
- أن الحالة تسمح بالانتقال المطلوب.

**الحكم:** مطابقة جزئية — **P1**.

**الإصلاح الآمن**

استخدام RPC/دالة المنصة الأصلية للتحقق وتغيير الحالة، أو إضافة تحقق خادمي يعتمد على العقد الموجود فقط. لا يكفي حماية الهاتف.

---

### 12.2 Push وRealtime على الأجهزة الفعلية غير مثبتان — P1

الهاتف يهيئ Push خارج Expo Go في `InternalCallContext.tsx:475-517` ويستخدم Realtime في `:519-573`.

لكن لا يوجد إثبات عملي لـ:

- الوصول عند إغلاق التطبيق.
- وضع توفير الطاقة.
- الشبكة الضعيفة.
- publication للجداول.
- صلاحيات قراءة `internal_calls`.
- native configuration في Production.

هذا بند اختبار فعلي وليس شيئاً يمكن إغلاقه بـTypeScript.

---

## 13. Realtime وStorage وProduction metadata

### 13.1 مخطط Realtime غير مثبت — P1

الكود يستخدم Realtime للطلبات، الرسائل، المكالمات والجلسات. لكن لا يمكن إثبات:

- أن الجداول منشورة في Realtime.
- أن payload لا يكشف بيانات طرف آخر.
- أن RLS تطبق على Realtime كما هو مطلوب.
- أن القنوات الواسعة لا تعيد تحميل بيانات غير لازمة.

### 13.2 Storage policies غير مثبتة — P0/P1 حسب bucket

يجب إثبات policies لكل:

- `chat-files`.
- `support-files`.
- `assignment-files`.
- `session-recordings`.

بدون ذلك لا يمكن إغلاق:

- عزل مرفقات الطالب.
- عزل مرفقات الحصة.
- صلاحية قراءة تسجيل الجلسة.
- منع رفع ملف في مجلد مستخدم آخر.

### 13.3 سبب عدم حسم Production

في جولة القراءة الحالية:

- `GET /rest/v1` مع OpenAPI أعاد `403`.
- probes المتوازية أعادت `429`.
- بعض الموارد أعادت `403`.

هذه النتائج لا تعني أن الجداول أو السياسات مفقودة؛ تعني أن مصدر metadata الحالي غير كافٍ للحكم.

---

## 14. البنود التي لا يجوز إصلاحها بالتخمين

لا يجوز تنفيذ أي من الآتي قبل إثبات العقد أو موافقة المستخدم على تغيير Production:

1. إنشاء RPC بديل لقبول الحجز.
2. إنشاء `sessions` من React بعد فشل القبول.
3. خصم دقائق من الهاتف.
4. إضافة أرباح المعلم من الهاتف.
5. إعادة الرصيد عند الإلغاء محلياً.
6. تحديث `teacher_score` أو `final_score` مباشرة دون contract.
7. جعل bucket خاصاً عاماً لتسهيل فتح الملفات.
8. اعتبار صفحة Stripe الناجحة دليلاً على الدفع.
9. اعتبار role المحلي دليلاً على اعتماد المعلم.
10. إضافة fallback إلى student عند فشل قراءة الدور.

## 15. ترتيب الإصلاح المقترح

### المرحلة الأولى — حماية المال والحجز

1. إثبات side effects لـRPCs القبول والرفض.
2. إثبات Trigger إنشاء `sessions`.
3. إثبات idempotency للتسوية.
4. إيقاف أو تعديل الإنشاء اليدوي في API حسب نتيجة العقد.
5. مطابقة مسار الإلغاء مع العقد الأصلي.

### المرحلة الثانية — الصلاحيات

1. فحص RLS وStorage وRealtime.
2. توحيد role + approval + ban في API.
3. اختبار الطالب والمعلم بحسابات حقيقية.

### المرحلة الثالثة — التعليم

1. بناء شاشة submission حقيقية.
2. دعم الملفات والإجابات.
3. ربط `grade-assignment`.
4. بناء ReviewSubmission واعتماد `final_score`.
5. استكمال CRUD للمواد والتسجيلات.

### المرحلة الرابعة — السحب والمكالمات

1. إثبات reserve السحب وتاريخ الحالات.
2. قراءة سعر الدقيقة من مصدر خادمي.
3. فحص authorization للمكالمات.
4. اختبار Push/Realtime على جهازين.

## 16. خلاصة الحكم

الأجزاء غير المطابقة بالكامل ليست كلها من النوع نفسه:

- **مخالفات مؤكدة من الكود:** تسليم الواجب الحقيقي، مراجعة المعلم، غياب CRUD المواد، إدراج bookings بعد قبول منفصل، الإلغاء المباشر، وعرض حالة السحب دون تاريخ.
- **فجوات مالية حرجة غير مثبتة:** خصم الجلسة، الأرباح، إنشاء session، شحن wallet، idempotency.
- **فجوات حماية وProduction:** RLS، Storage، Realtime، OAuth allow-list، Push، واعتماد المعلم.
- **جزء تم إصلاحه:** تخزين شهادات المعلم الجديدة كمسارات خاصة مع Signed URLs.

**النتيجة النهائية:** التطبيق لا يحقق parity كاملاً بعد. المسارات التي تمس الحجز والمال والهوية يجب أن تظل قراءة/عرضاً أو تستخدم عقد المنصة الأصلي المثبت، إلى أن يتم فحص Production واختبار حسابي طالب ومعلم على جهازين فعليين.