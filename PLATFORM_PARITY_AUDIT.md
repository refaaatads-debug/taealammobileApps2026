# PLATFORM_PARITY_AUDIT

**تاريخ التدقيق:** 2026-09-04  
**النطاق:** تطبيق `artifacts/ajyal-mobile`، خادم `artifacts/api-server`، عقود OpenAPI والأنواع المولدة، مخطط Drizzle المحلي، وما أمكن فحصه من مراجع المنصة الأصلية المنشورة.  
**نوع التدقيق:** READ-ONLY بالنسبة إلى Supabase Production وVPS وRPC؛ لم تُنفّذ كتابة اختبارية أو تعديلات على Schema أو RLS أو RPC أو بيانات Production.

## 0. قاعدة المصدر المرجعي

الملف المرفق `attached_assets/Pasted--1-GitHub-2-VPS-API--1788527627205_1788527627207.txt` يحدد المنصة الأصلية وبيانات Supabase وRPC وRLS كمصدر الحقيقة الوحيد. لذلك لا يُعد وجود شاشة أو endpoint مطابقاً دليلاً على parity؛ المعيار هو:

> نفس المستخدم + نفس البيانات + نفس Business Logic + نفس العمليات + نفس الصلاحيات + نفس النتيجة.

التدقيق الحالي يعتمد على الكود الموجود في مساحة العمل ومراجع المنصة التي أمكن الوصول إليها. لم يوجد كود صفحات الويب الأصلية كاملاً داخل مساحة العمل؛ `artifacts/mockup-sandbox` عبارة عن معرض معاينة عام وليس نسخة من واجهة المنصة. لذلك فنتائج UI الأصلية التي لا يمكن إثباتها من المصدر المتاح معلّمة بأنها **غير مثبتة** وليست مطابقة.

---

## A. صفحات الطالب

### الصفحات والمسارات المرئية

| الصفحة | المسار | الحالة الحالية |
|---|---|---|
| تسجيل الدخول/التسجيل | auth داخل `app/_layout.tsx` | Supabase Auth بالبريد/كلمة المرور، وGoogle موجود لكن يحتاج تحقق إعداد الإنتاج |
| الرئيسية | `/(tabs)/index` | تعرض الملف والجلسات والمهام والإشعارات وتربط بالأقسام |
| الجلسات والحجوزات | `/(tabs)/bookings` | جلسات قادمة/سابقة، طلبات الحجز، الإلغاء، Realtime |
| البحث عن معلم | `/find-teacher` | قائمة المعلمين والبحث وفلتر المرحلة محلياً |
| إنشاء الحجز | `/booking` | اختيار المعلم والمادة والوقت والمدة وإرسال طلب حجز |
| الباقات والاشتراك | `/subscriptions`, `/subscription` | قراءة الخطط والاشتراك والدقائق والشراء عبر `create-checkout` |
| الفواتير والمدفوعات | `/invoices` | قراءة `invoices` و`payment_records` |
| المهام والاختبارات | `/(tabs)/assignments` | قائمة المهام/الاختبارات وإكمال مهمة الطالب |
| الإشعارات | `/(tabs)/notifications` | قراءة، تعليم إشعار، تعليم الكل |
| الملف الشخصي | `/(tabs)/profile` | عرض الحساب، الخروج، الشروط والخصوصية |
| المواد | `/materials` | قراءة المواد والملفات والتسجيلات إن توفرت |
| المعلم الذكي | `/smart-teacher` | استدعاء Edge Function `ai-support` |
| المحادثة | `/chat` | قراءة/إرسال رسائل مرتبطة بالحجوزات |
| الدعم | `/support` | تذاكر ورسائل الدعم |
| التقييم | `/rating` | تقييم الجلسات المكتملة |
| الجلسة المباشرة | `/live-session` | غرفة مرتبطة بالحجز؛ النقل الأساسي صار مثبتاً على أنه نفس WebRTC/Realtime المستخدم في المنصة، مع فجوات في ميزات الجلسة المتقدمة |
| الشروط والخصوصية | `/terms`, `/privacy` | صفحات عامة |

### عمليات الطالب

- تسجيل الدخول والحفاظ على جلسة Supabase باستخدام SecureStore على الهاتف وAsyncStorage على الويب.
- قراءة الملف الشخصي والدور.
- رؤية الجلسات المرتبطة بـ `student_id`.
- رؤية وإرسال `booking_requests`.
- رؤية الباقات والدقائق والفواتير والمدفوعات.
- إلغاء الجلسة من واجهة التطبيق.
- إكمال مهمة بعد وجود `assignment_submissions`.
- قراءة الإشعارات وتعليمها كمقروءة.
- إرسال رسائل ومحادثات ودعم وتقييمات من خلال Supabase المباشر.
- استخدام `ai-support` للمعلم الذكي.
- استقبال المكالمات وقبولها/رفضها وإنهاؤها.

### فجوات الطالب

1. فلتر المرحلة في `find-teacher` محلي؛ endpoint المعلمين لا يثبت أن المرحلة أو المادة أو التوفر تُطبّق على المصدر نفسه.
2. شاشة الحجز تعرض جدولاً اصطناعياً محدوداً للأيام/الساعات، وليست بعد واجهة مصدر التوفر الرسمي كاملة.
3. فحص الرصيد في الواجهة يطابق الآن منطق المنصة المنشورة: الاشتراكات الفعالة ناقص حجوزات وطلبات الطالب ضمن أفق 14 يوماً؛ ما زال جدول التوفر نفسه يحتاج ربطاً كاملاً بالمصدر.
4. إضافة الموعد إلى التقويم ما زالت Alert وليست تكاملاً فعلياً مع تقويم الجهاز.
5. إعدادات التذكير والتركيز في الملف الشخصي محلية وغير محفوظة في المنصة.
6. إتمام المهمة في مسار Supabase يعيد نتيجة محسنة للواجهة لكنه لا يثبت تحديث سجل المهمة نفسه.
7. المحادثة والدعم والتقييم والمواد والاشتراك تستخدم Supabase مباشرة خارج عقد API، فلا يوجد عقد خادم موحد يمكن اختباره أو ضبط صلاحياته مركزياً.
8. النقل الأساسي داخل Expo يطابق عقد الويب، بما في ذلك تحسين SDP/Opus، معالجة تصادم العروض، إعادة إرسال join بعد عودة Realtime، وقناة بيانات `session-data`. المتبقي هو ربط القناة فعلياً بواجهة السبورة/الدردشة، التسجيل، ومراقبة جودة النقل.

---

## B. صفحات المعلم

### الصفحات والمسارات المرئية

| الصفحة | المسار | الحالة الحالية |
|---|---|---|
| الرئيسية | `/(tabs)/index` | لوحة عامة تختلف محتوياتها حسب الدور |
| الجلسات وطلبات الحجز | `/(tabs)/bookings` | طلبات مفتوحة وقبول/رفض وجلسات قادمة/سابقة |
| الطلاب | `/students` | قائمة الطلاب المرتبطين بالحجوزات/المهام، القراءة فقط |
| المحفظة والأرباح | `/wallet` | قراءة المحفظة والمعاملات والأرباح، بلا سحب |
| مواد المعلم | `/teacher-materials` | قراءة المواد والتسجيلات، بلا CRUD |
| المهام | `/(tabs)/assignments` | القائمة موجودة؛ مراجعة التسليمات/التقييم غير مكتملة |
| الملف والإشعارات والدعم والمحادثة | المسارات المشتركة | تختلف صلاحياتها وعملياتها حسب المصدر |

### عمليات المعلم الأصلية المثبتة

- رؤية طلبات `booking_requests` المفتوحة وغير المنتهية.
- تجميع الطلبات بحسب `group_id` عند وجود مجموعة.
- التحقق من وجود جلسة نشطة وتعارضات زمنية.
- قبول طلب منفرد عبر `accept_booking_request`.
- قبول مجموعة عبر `accept_booking_group`.
- رفض مجموعة من الطلبات عبر `reject_booking_request`.
- عند القبول: اختيار اشتراك الطالب الفعال، إنشاء booking بحالة `confirmed`، وإرسال إشعار ورسالة بداية المحادثة.
- رؤية الجلسات التي يشارك فيها المعلم.
- رؤية الطلاب المتصلين به من الحجوزات والمهام.
- قراءة أرباح المعلم عبر الدوال الأصلية مثل `get_teacher_earnings_breakdown` و`get_teacher_net_summary`.
- إدارة/قراءة المكالمات الداخلية عبر دوال المكالمات.

### فجوات المعلم

1. لا توجد في مساحة العمل نسخة صفحات الويب الأصلية للمعلم يمكن مقارنتها مكوّناً بمكوّن.
2. لا توجد عملية خادم لإدارة توفر المعلم أو تعديل أيام/ساعات العمل.
3. المحفظة والأرباح والسحب تعتمد على قراءة مباشرة من Supabase ولا يوجد API أو عقد صلاحيات/حالات للسحب.
4. مواد المعلم قراءة فقط؛ لا توجد إضافة أو تعديل أو حذف.
5. بطاقة الطالب في `/students` تفتح الآن محادثة مرتبطة بأقرب حجز لذلك الطالب؛ مراجعة التسليمات ما زالت غير مكتملة.
6. لا توجد عمليات إنشاء/تصحيح/مراجعة تسليمات المعلم في API.
7. لا توجد API مستقلة للمحادثة والتقييم والدعم والمحفظة والمواد.
8. جلسة الهاتف تثبت الانضمام إلى قناة الوسائط الأساسية، لكن جلسة المعلم على الهاتف محجوبة عمداً وتبقى إدارة البدء من الكمبيوتر كما في المنصة.

---

## C. جميع العمليات

### عمليات مشتركة أو موجودة حالياً

- المصادقة والخروج.
- قراءة الملف الشخصي.
- قراءة المعلمين والطلاب.
- قراءة/إنشاء الجلسات القديمة.
- إلغاء الجلسة.
- إنشاء طلب الحجز.
- عرض طلب الحجز.
- قبول/رفض طلب الحجز.
- قراءة/إكمال المهام.
- قراءة/تعليم الإشعارات.
- تسجيل جهاز Push وإرسال مكالمات Push.
- قراءة الخطط والاشتراكات والفواتير والمدفوعات.
- قراءة المحفظة والأرباح والمواد.
- إنشاء checkout.
- قراءة/إرسال chat وsupport وrating.
- استدعاء المساعد الذكي.
- عمليات RPC للمكالمات.

### عمليات مطلوبة وغير مكتملة أو غير مثبتة

- الخصم الذري للدقائق عند القبول أو بداية الجلسة.
- إرجاع الدقائق عند الإلغاء وفق سياسة المنصة.
- إنشاء سجل `session_compensations` عند عدم الحضور أو التعويض.
- إعادة الجدولة.
- الحالات التفصيلية لبداية الجلسة وتنفيذها وإنهائها.
- اكتمال parity لغرفة الصوت/الفيديو، بما في ذلك قناة البيانات والتسجيل ومعالجة إعادة التفاوض.
- سحب أرباح المعلم.
- CRUD لمواد المعلم.
- مراجعة تسليمات الطالب للمعلم.
- حفظ إعدادات الملف الشخصي.
- إضافة تقويم الجهاز.
- API موحد للدفع والمحفظة والمواد والمحادثة والدعم والتقييم.

---

## D. API وEndpoints

جميع المسارات التالية تُركّب تحت `/api` في `artifacts/api-server/src/routes/index.ts`:

### Health/Auth/Profile

- `GET /healthz`
- `GET /auth/user`
- `GET /login`
- `GET /callback`
- `GET /logout`
- `POST /mobile-auth/token-exchange`
- `POST /mobile-auth/logout`
- `GET /me`

### Directory

- `GET /teachers`
- `GET /students`

### Booking/Sessions

- `GET /sessions`
- `POST /sessions` — مسار قديم لإنشاء booking مباشرة، لا يمثل دورة المنصة الأصلية الكاملة.
- `PATCH /sessions/{id}/cancel`
- `GET /booking-requests`
- `POST /booking-requests`
- `PATCH /booking-requests/{id}/decision`

### Assignments/Notifications

- `GET /assignments`
- `PATCH /assignments/{id}/complete`
- `GET /notifications`
- `PATCH /notifications/{id}/read`
- `PATCH /notifications/read-all`

### Push/Calls

- `POST /push-tokens`
- `DELETE /push-tokens`
- `POST /calls/incoming`
- `POST /calls/{callId}/end`
- `POST /calls/{callId}/accept`

### نقص عقد API

لا توجد حالياً مسارات OpenAPI مستقلة للاشتراكات والخطط، checkout، الفواتير، المدفوعات، المحفظة، السحب، المواد، المحادثة، الدعم، التقييم، المساعد الذكي، حالات الجلسة التفصيلية، أو غرفة الاتصال.

النتيجة: بعض الشاشات تعمل ببيانات حقيقية، لكن خارج نفس قناة API والصلاحيات والاختبارات.

---

## E. RPC وEdge Functions

### RPCs الأصلية التي تم إثبات استخدامها

- `accept_booking_request(_request_id, _teacher_id)`
- `accept_booking_group(_group_id, _teacher_id)`
- `reject_booking_request(_request_ids, _teacher_id)`
- `start_internal_call(p_student_id, p_booking_id)`
- `respond_internal_call(p_call_id, p_accept)`
- `end_internal_call(p_call_id, p_reason)`
- `mark_internal_call_connected(p_call_id)`
- `set_new_user_role(_role)`
- `expire_internal_calls()`
- `get_teacher_earnings_breakdown(_teacher_id)`
- `get_teacher_net_summary(_teacher_id, _month)`

### Edge Functions

- `create-checkout`
- `ai-support`
- `turn-credentials`
- `session-report`

### ملاحظة سلامة

لم تُعدّل أي RPC أو Edge Function. مسار الخادم الجديد يستدعي RPC القبول/الرفض الأصليين، لكنه ما زال ينفذ إنشاء booking والإشعار والمحادثة في عمليات منفصلة؛ لذلك الذرية الكاملة تحتاج اختباراً مع RPC الأصلي أو واجهة أصلية توفر العملية كاملة.

### عقد غرفة الجلسة المثبت من مستودع المنصة

تمت قراءة مستودع المنصة الأصلي المتصل بـGitHub للقراءة فقط:

- المستودع: `refaaatads-debug/taealam`، الفرع `main`.
- `src/pages/LiveSession.tsx` يستورد `src/hooks/useWebRTC.ts`.
- hook الويب يطلب `turn-credentials` من Supabase Edge Function.
- الإشارات تستخدم Supabase Realtime Broadcast على القناة:
  `webrtc-${bookingId}`، مع الحدث `signal` وحمولة تتضمن `senderId` و`signalType` و`payload`.
- الوسائط نفسها WebRTC مباشر بين الطرفين عبر `RTCPeerConnection`، مع TURN صادر من `turn-credentials` وSTUN احتياطي.
- لم يظهر في مسار الجلسة هذا endpoint WebSocket أو Socket.IO أو LiveKit أو Jitsi أو Zoom مستقل على VPS.

بناءً على ذلك، عبارة «من خلال VPS» لا تثبت وجود بروتوكول غرفة آخر؛ قد يكون الـVPS طبقة الاستضافة أو البنية، لكن عقد الجلسة القابل للتنفيذ الذي تثبته المنصة هو Supabase Realtime + WebRTC + TURN. لا يجوز استبدال هذا النقل بعنوان VPS مُخمّن.

### مقارنة تطبيق الهاتف بعقد الويب

تطبيق الهاتف يستخدم بالفعل:

- نفس `webrtc-${bookingId}`.
- نفس حدث `broadcast/signal`.
- نفس `turn-credentials`.
- نفس `booking_id` كمفتاح الغرفة.
- نفس قاعدة عدم بدء دورة المعلم من الهاتف.

تمت مواءمة `useSessionWebRTC.ts` مع العقد الأساسي في `useWebRTC.ts`: ضبط Opus في SDP، معالجة offer glare وpolite peer، إعادة إرسال join بعد إعادة اتصال Realtime، وإضافة قناة البيانات `session-data` مع تخزين مؤقت محدود لأحداث السبورة. الفروق المتبقية هي ربط القناة بواجهة السبورة/الدردشة، تسجيل الجلسة، ومؤشرات جودة أو نوع مسار ICE؛ لذلك parity النقل الأساسي مثبت، بينما parity تجربة الجلسة الكاملة غير مثبت.

---

## F. الجداول والعلاقات

### جداول Supabase المستخدمة أو المتوقعة

| المجال | الجداول/العلاقات |
|---|---|
| الهوية | `profiles`, `public_profiles`, `user_roles` |
| المعلمون | `public_teacher_profiles`, `teacher_profiles`, `teacher_subjects`, `subjects` |
| الحجز | `booking_requests`, `bookings`, وعلاقة `booking_requests.subject_id → subjects.id` |
| الاشتراكات | `subscription_plans`, `user_subscriptions` |
| الجلسات والتعويض | `sessions`, `session_compensations` |
| الدفع | `invoices`, `payment_records` |
| المحفظة | `wallets`, `wallet_transactions`, `teacher_earnings`, `withdrawal_requests` |
| المهام | `assignments`, `assignment_submissions` |
| الإشعارات | `notifications` |
| الاتصال/المحادثة | `internal_calls`, `chat_messages` |
| المواد | `session_materials` وStorage bucket `session-recordings` |
| التقييم | `reviews` |
| الدعم | `support_tickets`, `support_messages` |

### النموذج المحلي البديل

`lib/db/src/schema` يغطي نموذجاً منفصلاً ومبسّطاً:

- `users`
- `sessions`
- `bookings`
- `assignments`
- `notifications`
- `push_tokens`

هذا النموذج مفيد لمسار OIDC/الاختبارات المحلية، لكنه ليس نموذج Production الأصلي؛ استعماله كمسار fallback يعني أن سلوك الطلب قد يختلف إذا لم يصل Bearer صالح.

### علاقات وحالات تحتاج تثبيتاً قبل الادعاء بالتطابق

- علاقة `booking_request` بالحجز المؤكد بعد قبول الطلب.
- طريقة ربط `subscription_id` بالخصم والرد.
- علاقة `sessions` بالحجز وغرفة الاتصال.
- حالات `session_status` التفصيلية.
- قيود `session_compensations`.
- سياسات RLS الفعلية لجداول Production غير الموجودة في مساحة العمل.

---

## G. الحالات Status

### عقود التطبيق الحالية

- الدور: `student | teacher`
- طلب الحجز: `open | accepted | rejected | cancelled | expired`
- الجلسة في API: `upcoming | done | cancelled`
- المهمة: `قيد التقدم | لم يبدأ | مكتمل`
- نوع المهمة: `واجب | اختبار`
- نغمة العرض: `teal | gold | navy`
- المكالمة في الهاتف: `ringing | active | declined | ended`
- حالة الاشتراك في العرض: `فعال | منتهي | موقوف | غير مشترك`

### حالات Production التي ظهرت في المنطق الأصلي

- booking: `pending | confirmed`
- booking legacy/local: `scheduled | completed | cancelled`
- session: `waiting_acceptance | in_progress` إضافة إلى الحالات النهائية
- internal call: `ringing | connecting | connected | rejected | busy | cancelled | missed | ended | failed`
- subscription: `is_active`, `remaining_minutes`, `ends_at`

### اختلافات الحالة

توجد طبقة تحويل بين الحالات المحلية والبعيدة؛ مثلاً `confirmed` تتحول إلى `upcoming` في API. هذا مناسب للعرض، لكنه يفقد حالات تشغيلية مهمة مثل الانتظار والاتصال وعدم الحضور، ويمنع واجهة الهاتف من عكس كل نتيجة أصلية بدقة.

---

## H. الصلاحيات وRLS

### ما هو مثبت في الكود

- Bearer Supabase يُفحص قبل جلسة OIDC القديمة.
- الدور teacher يُثبت عبر وجود صف في `user_roles`.
- قائمة الطلبات الواردة وقبول/رفض الطلب للمعلم فقط.
- إنشاء الحجز/الطلب للطالب فقط.
- قائمة الطلاب للمعلم فقط.
- مسار fallback المحلي يستخدم `app.user_id` وسياسات Drizzle RLS.
- إشعارات ومسارات Push المحلية تقيد الوصول بالمالك أو server-write.
- مسارات Supabase تعتمد على RLS الخاص بـJWT.

### ما لم يمكن إثباته READ-ONLY من مساحة العمل

- تعبيرات `USING` و`WITH CHECK` الفعلية لـRLS في Production.
- صلاحيات كل جدول مباشر تستخدمه شاشة الاشتراك/المحفظة/المواد/الدعم.
- ما إذا كان كل RPC يستخدم `SECURITY DEFINER`، وما هي قيود مالك الطلب داخله.
- مطابقة هوية Google/Supabase مع حسابات OIDC القديمة في كل الحالات.

### مخاطر مكتشفة

1. إلغاء Supabase يتحقق الآن صراحةً من أن المستخدم أحد طرفي الحجز، لكنه لا ينفذ بعد سياسة رد الدقائق الأصلية.
2. قناة Realtime للحجوزات مفلترة الآن للطالب ولطرف الحجز؛ الطلبات المفتوحة الواردة للمعلم لا يمكن فلترتها بالمعلم إذا لم يتضمن الصف `teacher_id`.
3. مسارا تعليم الإشعار يستخدمان الآن `is_read` الموحد؛ يجب تأكيد أن هذا هو العمود الفعلي في Production.
4. المسارات المباشرة من الهاتف تتجاوز عقد الخادم، ما يصعّب فرض صلاحيات موحدة.
5. النموذج المحلي يسمح بسلوك مختلف عن Production إذا فُقد Bearer.

---

## I. الفرق بين المنصة والتطبيق الحالي وسبب كل اختلاف

| المجال | الفرق | السبب |
|---|---|---|
| الصفحات الأصلية | لا توجد نسخة Web أصلية كاملة داخل workspace | المصدر المتاح محلياً API/mobile وليس repository الويب الكامل |
| الحجز | كان إنشاء جلسة مباشرة؛ تم إضافة دورة الطلب والقبول | التطبيق بدأ بنموذج مبسط قبل اكتشاف `booking_requests` وRPCs الأصلية |
| القبول | يستخدم RPC الأصلي، ثم إنشاء booking منفصل | لا يوجد endpoint أصلي موحد مكشوف لعقد الهاتف |
| الدقائق | فحص وحقول اشتراك موجودة، بلا خصم/رد موثق | منطق الخصم والتعويض الأصلي غير ممثل في API الحالي |
| الإلغاء | يكتب الآن status/session_status وحقول من ألغى ووقت الإلغاء والسبب | سياسة رد الدقائق والإشعار الأصلي غير موصوفة في العقد الحالي |
| الدفع | checkout والفواتير من Supabase المباشر | لم تُنشأ طبقة API/OpenAPI لهذه الوظائف |
| المحفظة | قراءة فقط | لا يوجد عقد سحب أو صلاحية teacher wallet في الخادم |
| المواد | قراءة وتنزيل توقيع مؤقت | لا توجد CRUD endpoints للمعلم |
| المهام | الطالب يستطيع الإكمال؛ مراجعة المعلم غير موجودة | API الأصلي الموجود يغطي قائمة/إكمال فقط |
| الإشعارات | عدم اتساق `is_read/read_at` | مساران تطورا بشكل منفصل |
| المكالمات | RPC وحالة Push موجودان؛ الوسائط غير موجودة | Expo الحالي لا يحتوي طبقة نقل صوت/فيديو مثبتة |
| Realtime | الفوترة مفلترة؛ الحجوزات غير مفلترة؛ مجالات أخرى بلا Realtime | تمت إضافة freshness تدريجياً ولم تُستخرج كل قنوات المنصة |
| Auth | الهاتف Supabase PKCE والخادم يدعم OIDC cookie fallback | انتقال مرحلي، ويحتاج اختبار هوية/دور شامل |
| API contract | بعض البيانات الحقيقية لا تمر عبر OpenAPI | بناء سريع لواجهات القراءة المباشرة قبل توحيد الخادم |
| local fallback | نموذج مستقل عن Production | مطلوب للتوافق الخلفي مع OIDC، لكنه ليس parity مع Supabase |

---

## J. خطة الإصلاح بالترتيب المطلوب

### P0 — قبل اعتبار مسار الحجز صالحاً للإطلاق

1. اختبار قبول/رفض الطلب عبر حساب طالب وحساب معلم منفصلين.
2. التحقق من RPCs الأصلية دون تعديلها، وقياس نتيجة القبول المتزامن.
3. توحيد خصم الدقائق والرد والتعويض عبر الدوال الأصلية فقط.
4. ربط الإلغاء بسياسة المنصة وإشعار الطرف الآخر وإرجاع الدقائق عند استحقاقه.
5. منع قبول طلب بلا رصيد أو بموعد متعارض أو بعد الانتهاء.
6. تأكيد إنشاء booking واحد فقط لكل request.

### P1 — عقد الخادم والبيانات

1. إنشاء API/OpenAPI موحد للاشتراكات والخطط والفواتير والمدفوعات والمحفظة والمواد والمحادثة والدعم والتقييم.
2. نقل العمليات المباشرة من الهاتف إلى API أو توثيقها بعقود generated مع حراسة صلاحيات صريحة.
3. إضافة حالات الجلسة التفصيلية: الانتظار، البدء، التنفيذ، الإنهاء، الإلغاء، عدم الحضور.
4. إضافة عقود teacher assignments/review/materials/wallet withdrawals.
5. توحيد `is_read` أو `read_at` وفق العمود الأصلي، وعدم تحديث عمود غير مؤثر.

### P1 — التحديث اللحظي

1. فلترة Realtime بحسب المستخدم/المشارك.
2. إضافة freshness للإشعارات والمهام والمحفظة والمواد والمحادثة وحالة المكالمة.
3. إعادة الاشتراك بعد انقطاع الشبكة ومنع التحديثات المكررة.

### P1 — غرفة الجلسة والمكالمات

1. تم تحديد transport الرسمي: WebRTC مباشر مع Supabase Realtime signaling وTURN من `turn-credentials`.
2. تم تنفيذ Join/Leave الفعلي في عميل الهاتف مع حماية offer glare، إعادة الاتصال، وقناة بيانات؛ ما زال يلزم اختبار Development Build وجهاز فعلي.
3. ربط `booking_id` و`internal_call` و`session` في النتيجة الأصلية.
4. جعل قبول/رفض/إنهاء المكالمة عمليات مؤكدة وليست fire-and-forget.
5. اختبار الشبكة الضعيفة، الخلفية، إغلاق التطبيق، Android وiOS.

### P2 — اكتمال واجهة المعلم والطالب

1. ربط بطاقة الطالب بإجراء فعلي.
2. إضافة إدارة توفر المعلم.
3. إضافة مراجعة التسليمات والتقييمات.
4. إضافة CRUD مواد المعلم.
5. حفظ إعدادات الملف الشخصي وإضافة تقويم الجهاز.
6. توحيد رسائل النجاح والفشل مع نتائج المنصة الأصلية.

### بوابات التحقق لكل دفعة

- `pnpm run typecheck`
- `git diff --check`
- build لخادم API وExpo
- health/API checks دون بيانات إنتاج
- مراجعة عدم وجود Mock Data
- مراجعة أن كل عملية تستخدم نفس endpoint/RPC الأصلي
- اختبار الصلاحيات بحساب طالب ومعلم
- اختبار القراءة بعد reload وفي جلسة ثانية
- لا Schema changes ولا RLS changes ولا RPC changes

## الخلاصة التنفيذية

التطبيق ليس نظاماً منفصلاً من ناحية الهوية أو مصادر البيانات الأساسية، لكنه **ليس مطابقاً 100% بعد**. تم إثبات وتنفيذ النقل الأساسي لغرفة الجلسة بما يطابق المنصة الأصلية (Supabase Realtime signaling + WebRTC + TURN)، وليس endpoint VPS مستقلاً. ما زالت واجهة السبورة/الدردشة داخل الغرفة، التسجيل، جودة ICE، والخصم/الإرجاع والتعويض، عمليات المعلم الثانوية، وعقود API الموحدة تحتاج تنفيذ أو اختباراً قبل إعلان parity كامل.

---

## K. دليل اللقطات والفحص العام بتاريخ 2026-09-04

### ما تم إثباته من النطاق الأصلي

- `GET https://ajyalalmaerifa.com/login` يعيد صفحة الواجهة بحالة `200`.
- Auth العام في Supabase يستجيب بحالة `200` عند استخدام المفتاح العام الموجود في حزمة الواجهة.
- `POST /api/auth/session` ليس مسار Supabase Auth في هذا النطاق، ويعيد `405 Not Allowed` من Nginx؛ لذلك لا يصح تشخيصه كمسار تسجيل الدخول الأصلي.
- جذر PostgREST `GET /rest/v1/` أعاد:
  - الحالة `500`
  - `code: 57014`
  - `message: canceling statement due to statement timeout`
- استعلامات `limit=0` لجداول `user_roles`, `profiles`, `public_profiles`, `public_teacher_profiles`, `subscription_plans`, `user_subscriptions`, `booking_requests`, `bookings`, و`notifications` أعادت `200`، لكن بزمن يقارب ثانيتين لكل استعلام.

### الاستنتاج الآمن

الدليل الحالي يشير إلى بطء أو مهلة في طبقة PostgREST/الاستعلامات، وليس إلى تلف واضح في Auth أو اختفاء الجداول. لا يمكن تحديد الاستعلام الذي ظهر كـ`500` في لقطة تسجيل الدخول من الصورة وحدها، ولا يجوز إصلاحه بتعديل RLS أو Schema أو إعداد OAuth قبل الحصول على URL الطلب الفاشل وسجل الخادم.

### قيود الوصول

- المستودع المحلي يحتوي كود تطبيق الهاتف وخادم API المشترك، وليس مستودع الويب الأصلي كاملاً.
- DNS للنطاق يشير إلى `72.61.23.237`، لكن اتصال SSH القراءة فقط لم ينجح بالمفتاح المخزن بصيغته الحالية أو بحسابي `root` و`ubuntu`.
- لم يتم تعديل VPS أو Nginx أو Supabase Production أو بيانات المستخدمين.

### الإجراء المطلوب قبل إصلاح 500

1. الحصول على اسم مستخدم SSH ومسار خدمة المنصة أو سجل Nginx/PostgREST وقت الخطأ.
2. استخراج URL الطلب الفاشل من Network في المتصفح، دون مشاركة Cookies أو Access/Refresh Tokens.
3. إعادة إنتاج الطلب نفسه بقراءة فقط إن كان GET، ثم تحديد إن كانت المهلة من PostgREST أو من استعلام/سياسة محددة.
4. عدم تنفيذ Rollback أو تعديل OAuth/RLS/Schema قبل إثبات السبب ومراجعة النسخة السليمة.
