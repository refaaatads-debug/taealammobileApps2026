# تقرير تدقيق تطابق منصة أجيال المعرفة

**تاريخ التدقيق:** 2026-09-05  
**النطاق:** تطبيق Expo للطالب والمعلم، API server، وثائق العمارة المرفقة، ومحاولة قراءة Supabase Production عبر الاتصال المثبت.  
**قاعدة التغيير:** لم تُنفذ أي كتابة أو دفع أو DDL أو تعديل على Schema أو RLS أو RPC أو Trigger أو Auth/OAuth أو VPS.

## 1. الخلاصة التنفيذية

التطبيق يلتزم في أجزاء مهمة بمصدر الحقيقة الأصلي: يستخدم جلسة Supabase، يرسل طلبات الحجز إلى `booking_requests`، يستخدم RPCs القبول والرفض، ويستخدم Supabase Realtime وWebRTC للجلسات. لكنه لا يمكن اعتباره مطابقاً كاملاً بعد.

أعلى المخاطر:

1. **P0 — الحجز والقبول:** فحص الرصيد والتعارض ثم استدعاء RPC ثم إنشاء `bookings` وإرسال الإشعارات تتم في طلبات منفصلة. لا يوجد إثبات من الاتصال الحالي أن RPCs Production تنشئ الحجز والجلسة داخلياً، وبالتالي قد توجد نافذة قبول بلا حجز أو قبولين متنافسين.
2. **P0 — تسوية الجلسة:** لا يوجد من الكود وحده إثبات أن إنهاء الجلسة يمر عبر Trigger/RPC الأصلي مرة واحدة، أو أن خصم الدقائق وأرباح المعلم idempotent.
3. **P0 — الصلاحيات وProduction:** لا يمكن إثبات RLS وStorage وRealtime وTriggers وقيود uniqueness وRPC signatures من الاتصال الحالي.
4. **P1 — الإلغاء:** مسار API يلغي `bookings` مباشرة عبر PATCH، بينما الوثائق تجعل انتقالات الحالة والتسوية منطقاً خادمياً حساساً.
5. **P1 — مهام المعلم:** الهاتف يقرأ `assignment_submissions` فقط؛ لا توجد دورة مراجعة واعتماد درجة أو `final_score`.
6. **P1 — السحب:** التطبيق يعرض حالات محددة فقط، ولا يقرأ `withdrawal_status_history`، ولا يثبت إشعار الإدارة أو حجز مبلغ السحب ذرياً.
7. **P1 — مواد/تسجيلات:** القراءة موجودة جزئياً، لكن دورة CRUD الكاملة للمعلم غير مثبتة.

لا يُسمح بإغلاق هذه البنود على أنها ناجحة إلا بعد قراءة Production بصلاحية آمنة مناسبة أو فحص مباشر من لوحة Supabase، ثم اختبار حسابين حقيقيين وجهازين فعليين للمسارات الحساسة.

## 2. منهجية التحقق

### ما تم فحصه

- وثائق:
  - `platform-master-integrated-architecture_1788629894258.md`
  - `full-session-booking-finance-assignments-architecture_1788629894391.md`
  - `student-dashboard-architecture_1788629894440.md`
  - `teacher-dashboard-architecture_1788629894486.md`
- API:
  - `artifacts/api-server/src/routes/learning.ts`
- الهاتف:
  - طلبات الحجز، الجلسات، المهام، الملف الشخصي، السحب، المحفظة والمحادثة.
- تحقق قراءة فقط عبر اتصال Supabase المثبت.
- `typecheck` و`git diff --check` والـ Metro والمعاينة البصرية، كما هو موثق في حالة المشروع.

### حدود اتصال Supabase الحالي

| الفحص | النتيجة | التفسير |
|---|---:|---|
| `GET /rest/v1` مع OpenAPI | `403` | نقطة OpenAPI محجوبة من الاتصال الحالي؛ لا تثبت غياب Schema |
| قراءات PostgREST متوازية | عدد من `429` | تقييد معدل من البوابة؛ لا تستخدم هذه النتائج لإثبات وجود/غياب جدول |
| بعض الموارد | `403` | صلاحية الاتصال أو سياسة المورد تمنع القراءة؛ لا يمكن استنتاج RLS الفعلية |
| بيانات الصفوف | لم تُعرض | التزم الفحص بعدم إخراج بيانات Production أو أسرار |

**النتيجة:** فحص Production في هذه الجولة **غير حاسم**. نحتاج اتصالاً بصلاحية قراءة مناسبة أو تصديراً من لوحة Supabase يتضمن metadata فقط. لا ينبغي رفع الصلاحية أو تغيير المفتاح تلقائياً من التطبيق.

## 3. جدول المطابقة

الحالات المستخدمة:

- **مطابق:** الدليل الحالي يكفي لتطابق السلوك المطلوب.
- **جزئي:** جزء من السلوك موجود، لكن توجد فجوة أو اعتماد غير مثبت.
- **مخالف:** يوجد مسار فعلي يناقض الوثيقة أو يتجاوز مصدر الحقيقة.
- **يحتاج تحقق:** لا يمكن إثباته من الكود أو من اتصال Production الحالي.

| المتطلب | التطبيق الحالي | Supabase/API الفعلي الظاهر | الحالة | الخطورة | الملف/الموضع | خطة الإصلاح دون Schema |
|---|---|---|---|---|---|---|
| إنشاء طلب الطالب في `booking_requests` | API يتحقق من الملف والاشتراك والمعلم والمادة والتكرار ثم يدرج الطلب | `POST /booking-requests`، لكن الفحوص والإدراج منفصلة | جزئي | P1 | `api-server/src/routes/learning.ts:385-528` | إبقاء الإنشاء عبر العقد الأصلي؛ بعد تثبيت الأعمدة والقيود انقل منع التكرار إلى RPC/قيد موجود إن كان متاحاً، وإلا لا تدّع الذرية |
| قبول فردي ذري مع lock وbalance وconflict وbooking/session | API يستدعي `accept_booking_request` ثم يدرج `bookings` يدوياً | وجود RPC ظهر في الكود، لكن signature وside effects غير مقروءة من Production | مخالف/يحتاج تحقق | P0 | `learning.ts:588-693` | فحص نتيجة RPC الفعلية؛ إذا كان RPC ينشئ booking/session احذف الإنشاء اليدوي. إذا لم يكن كذلك، لا يمكن إصلاح الذرية من العميل؛ يلزم RPC/Edge Function معتمد من المستخدم |
| قبول المجموعة ومنع القبول المكرر | API يستدعي `accept_booking_group` ثم يعيد فحصاً محلياً ويدرج المفقود | لا يوجد إثبات لقيد unique أو transaction واحدة | جزئي | P0 | `learning.ts:614-693` | الاعتماد على RPC ذري مع idempotency؛ جعل القراءة اللاحقة verification فقط لا مسار إنشاء |
| رفض الطلب | API يستخدم `reject_booking_request` | RPC مستخدم، signature غير مثبتة | جزئي | P1 | `learning.ts:732-740` | قراءة contract/permissions، ثم إبقاء rejection خادمياً |
| فحص الرصيد قبل القبول | API يقرأ `user_subscriptions` ثم RPC | القراءة ليست lock ولا ضماناً لعدم تغيّر الرصيد | مخالف | P0 | `learning.ts:588-604` | نقل القرار النهائي إلى RPC الأصلي أو عقد خادمي ذري مثبت؛ لا تعتمد على فحص React/API |
| جلسة المعلم | الهاتف يمنع بدء المعلم ويترك دورة الجلسة للمنصة | WebRTC وRealtime موجودان، لكن Trigger التسوية غير مثبت | جزئي | P0 | `live-session.tsx`, `useSessionWebRTC.ts` | لا تغيّر النقل؛ تحقق من `sessions`, `active_sessions`, `session_events`, triggers وEdge Functions |
| إنهاء الجلسة والخصم والأرباح | التطبيق يقرأ النتائج، والكود لا يثبت idempotency للتسوية | الوثائق تتطلب `auto_complete_session` وتسوية واحدة | يحتاج تحقق | P0 | `ConnectedSectionScreen.tsx`, جلسة الويب الأصلية | إثبات trigger/RPC وقيود uniqueness من Production، ثم أصلح النقل/العرض فقط |
| إلغاء الحجز | API يحدث `bookings` مباشرة ويكتب إشعاراً بعد ذلك | الوثائق تفرض انتقالات حالة وتسوية خادمية | مخالف/يحتاج تحقق | P1 | `learning.ts:1022-1171` | تحديد RPC الإلغاء الأصلي إن كان موجوداً واستخدامه؛ لا تضف refund محلياً |
| دور الطالب/المعلم | API يفحص Supabase role، والهاتف لديه identity gates | RLS والسياسات غير قابلة للقراءة من الاتصال الحالي | جزئي | P0 | `learning.ts`, `lib/auth.tsx`, `useAjyal` | إبقاء الحارس الصريح، ومنع fallback؛ فحص RLS وrole claims وapproval في Production |
| حارس اعتماد المعلم | بطاقة المعلم تعرض `teacherApproved`، وبعض API paths تتحقق من role فقط | لا يوجد إثبات أن كل مسارات المعلم تشترط `is_approved` | جزئي | P1 | `learning.ts`, `app/_layout.tsx` | توحيد guard على الخادم والراوتر حسب العقد الأصلي؛ لا تمنح اعتماداً محلياً |
| محفظة الاتصال منفصلة | شاشة منفصلة للمعلم، wallet/logs وStripe Functions موجودة | سعر الدقيقة معروض كـ`0.30` ثابت في UI | جزئي | P1 | `TeacherFinanceScreens.tsx:233-330` | قراءة إعداد السعر من العقد الخادمي الموجود بعد إثبات اسمه؛ لا تحسب التكلفة على الهاتف |
| شحن المحفظة | يبدأ checkout ثم يستدعي `wallet-topup-verify` بعد العودة | نجاح صفحة العودة ليس دليلاً كافياً لتفعيل الرصيد | جزئي/يحتاج تحقق | P0 | `TeacherFinanceScreens.tsx:264-288` | التحقق من webhook/Edge Function ومفتاح idempotency في Production؛ لا تضف credit من React |
| أرباح المعلم | يقرأ breakdown وearnings وsummary | RPCs مستخدمة، لكن صلاحياتها ودفترها غير مثبتين | جزئي | P1 | `TeacherFinanceScreens.tsx:81-107` | فحص RPC results وRLS؛ إبقاء الحسابات العرضية بلا تعديل مالي |
| طلب السحب | يرفع path خاصاً، ثم يدرج `withdrawal_requests` ويشعر المعلم | إشعار المعلم نفسه موجود؛ إشعار الإدارة وتجميد الرصيد غير مثبتين | جزئي | P1 | `TeacherFinanceScreens.tsx:133-184` | تثبيت contract الإدارة والـRPC إن وجد؛ لا يخصم أو يحجز المبلغ من الهاتف |
| تاريخ السحب | يعرض status الحالي فقط | `withdrawal_status_history` مطلوب بالوثيقة وغير مقروء | مخالف/يحتاج تحقق | P1 | `TeacherFinanceScreens.tsx:217-225` | قراءة الجدول بعد إثبات أعمدته، وعرض الحالات غير المعروفة صراحة |
| مرفقات السحب | المسار الخاص وSigned URL عند الفتح | متوافق مع bucket خاص، مع بقاء الروابط القديمة مدعومة | مطابق جزئياً | P1 | `TeacherFinanceScreens.tsx:34-49,147-159` | فرض الحجم والنوع، وعدم تحويل المسار إلى public URL |
| شهادات المعلم | قبل الإصلاح كانت تحفظ `getPublicUrl` | bucket الخاص لا ينبغي أن يعتمد على public URL | مخالف | P1 | `app/(tabs)/profile.tsx` | تم إصلاحه في هذه الجولة إلى path + Signed URL؛ يبقى فحص Storage policy |
| واجبات الطالب | API يتحقق من وجود submission عند complete | لا يوجد في الهاتف مسار عرض كامل للأسئلة/الإجابة/المرفقات | جزئي | P1 | `learning.ts:1173-1290`, `assignments.tsx` | ربط UI بعقد assignment الأصلي؛ لا تعتبر `complete` إنشاء submission |
| مراجعة المعلم | Modal يقرأ `assignment_submissions` فقط | لا يوجد اعتماد `teacher_score/final_score` أو إشعار الطالب | مخالف | P1 | `assignments.tsx:35-53,111-136` | إضافة المسار فقط بعد تثبيت Edge Function/RPC والحقول؛ لا تخمّن أسماء الأعمدة |
| AI grading | غير موجود في الهاتف | الوثيقة تتطلب `grade-assignment` وحالات `ai_graded/reviewed` | يحتاج تحقق | P1 | `assignments.tsx` | استدعاء Edge Function الأصلية بعد تثبيت contract، لا نسخ منطق AI إلى العميل |
| المواد والتسجيلات | توجد قراءات في `ConnectedSectionScreen` والجلسة | دورة teacher CRUD الكاملة غير ظاهرة في mobile | جزئي | P1 | `ConnectedSectionScreen.tsx`, `SessionCollaboration.tsx` | مطابقة bucket/fields وعمليات CRUD الأصلية، ثم تنفيذ UI |
| الرسائل والمرفقات | Realtime وprivate `chat-files` وSigned URLs موجودة | الاختبار بحسابين حقيقيين غير منفذ | جزئي | P1 | `MessagesScreen.tsx` | اختبار حساب طالب/معلم، وإبقاء المسارات booking-scoped |
| Realtime | قنوات لبعض الجداول والطلبات والجلسات | publication والـpolicies غير مثبتة | يحتاج تحقق | P1 | `useRealtimeRefresh`, `InternalCallContext` | فحص publication وRLS وpayload من Supabase |
| OAuth/Deep Links/Push | كود Google وdeep links وpush موجود | إعدادات Production غير مقروءة | يحتاج تحقق | P1 | `lib/auth.tsx` | اختبار بيئة Production وجهاز فعلي؛ لا تغيّر OAuth من التطبيق |

## 4. تفاصيل المخاطر الحرجة

### 4.1 القبول والإنشاء المنفصل

الوثيقة تصف التدفق التالي كعملية واحدة: lock، فحص الرصيد والتعارض، تحديث الطلب، إنشاء الحجز، إنشاء الجلسة، ثم الإشعار. الكود الحالي:

1. يقرأ الطلب والاشتراك والتعارض.
2. يستدعي RPC القبول.
3. يقرأ الطلبات المقبولة.
4. يقرأ الاشتراك والحجوزات الموجودة.
5. يدرج `bookings` يدوياً.
6. يرسل الإشعارات ورسالة الترحيب.

هذه ليست معاملة واحدة من منظور API. إعادة المحاولة قد تكون idempotent جزئياً بسبب فحص الحجز، لكن ذلك ليس بديلاً عن transaction أو unique constraint مثبتة. الإصلاح الآمن يتطلب معرفة ما تفعله RPCs الموجودة فعلياً، ولا يجوز اختراع RPC جديد أو تعديل Production دون موافقة صريحة.

### 4.2 مسار الجلسة والتسوية

الوثائق تجعل `sessions` و`active_sessions` و`session_events` وTrigger التسوية مصدر الحقيقة. التطبيق يعرض `deducted_minutes` و`short_session` ويستخدم النقل الأصلي، لكن لا يوجد في كود الهاتف إثبات أن:

- إنهاء الجلسة لا يمكن تكراره مالياً.
- الجلسة القصيرة تُسوّى بالقواعد الأصلية.
- `teacher_earnings` ودفتر الاشتراك يتغيران في العملية نفسها.
- cancellation لا يخلق refund غير موجود في المنصة.

لذلك أي عرض للرصيد في الهاتف يجب أن يبقى قراءة فقط.

### 4.3 الإلغاء المباشر

مسار `PATCH /sessions/:id/cancel` يقرأ الحجز ثم يكتب `status`, `session_status` وحقول الإلغاء مباشرة. قد تكون RLS وTriggers قادرة على حماية ذلك، لكن الوثائق لا تمنح دليلاً كافياً على أن هذا هو العقد الأصلي. لا ينبغي إضافة refund أو إعادة دقائق محلياً.

## 5. ما تم إصلاحه في هذه الجولة

- شهادات المعلم لا تحفظ رابطاً عاماً جديداً.
- المسار يحفظ داخل `support-files`، ويُنشئ Signed URL لمدة قصيرة عند الفتح.
- أضيف رفض للامتدادات غير المدعومة وحد أقصى للحجم قبل إدخال الملف.
- بقي دعم الروابط القديمة الموجودة مسبقاً حتى لا تتعطل السجلات التاريخية.

## 6. خطة الإصلاح المرحلية دون تغيير Schema

### المرحلة A — إثبات العقود

1. الحصول على قراءة metadata آمنة من Supabase: الجداول، الأعمدة، enums، foreign keys، unique constraints، RPC signatures، triggers، policies، Storage buckets/policies وRealtime publication.
2. مطابقة نتائج Production مع أسماء RPCs التي تستعملها `learning.ts` وEdge Functions.
3. توثيق نتيجة كل RPC: parameters، return shape، side effects، صلاحيات التنفيذ، وقابلية إعادة المحاولة.
4. عدم تشغيل RPC تغييري أثناء هذا الفحص.

### المرحلة B — الحجز والجلسة

1. إذا كان RPC القبول ينشئ الحجز والجلسة: إزالة الإنشاء اليدوي من API والإبقاء على read-after-write للتحقق.
2. إذا كان لا ينشئهما: إيقاف مسار القبول مؤقتاً أو طلب اعتماد تغيير خادمي ذري في المنصة الأصلية؛ لا تعالج ذلك من React.
3. استبدال الإلغاء المباشر بعقد المنصة الأصلي إن كان مثبتاً.
4. فحص idempotency للتسوية، ثم اختبار حساب طالب ومعلم بحجز واحد وحجزين متنافسين.

### المرحلة C — الصلاحيات والمال

1. توحيد teacher role + approval + ban guards في API والراوتر مع إبقاء RLS مصدراً نهائياً.
2. جعل سعر الدقيقة قراءة من إعداد خادمي مثبت، وليس ثابتاً في الواجهة.
3. ربط السحب بتاريخ الحالات وإشعار الإدارة فقط بعد تثبيت contract.
4. الإبقاء على webhook/Edge Function مصدر تفعيل الدفع والـcredit.

### المرحلة D — المهام والمواد

1. إضافة ReviewSubmission بعد إثبات الحقول وEdge Function.
2. تنفيذ UI للإجابة والمرفقات واستدعاء `grade-assignment` الأصلي.
3. إكمال CRUD للمواد والتسجيلات عبر العقد الأصلي، دون كتابة مباشرة غير موثقة.

### المرحلة E — التحقق الحقيقي

هذه ليست نتائج من الكود:

- طالب ومعلم حقيقيان.
- جهازان فعليان.
- حسابا Google وDeep Link Production.
- شبكة ضعيفة ووضع توفير الطاقة.
- رسالة ومرفق PDF/صورة.
- طلب قبول ورفض، حجزان متنافسان، جلسة مكتملة، جلسة قصيرة، إلغاء، وسحب.

## 7. قائمة الأدلة المطلوبة لإغلاق التقرير

- [ ] نتيجة `pg_policies` للجداول الحساسة.
- [ ] نتيجة `pg_trigger` واسم كل Trigger للتسوية والقبول والإشعارات.
- [ ] foreign keys وunique constraints لـ`booking_requests`, `bookings`, `sessions`, `assignment_submissions`, `withdrawal_requests`.
- [ ] signatures ونتائج RPCs: القبول، الرفض، التسوية، الأرباح، wallet، وEdge Functions.
- [ ] Storage policies وخصوصية `support-files`, `chat-files`, `assignment-files`.
- [ ] Realtime publication وpolicies للجداول المتزامنة.
- [ ] إعدادات webhook الدفع، TURN، Twilio، OAuth وDeep Links Production.
- [ ] نتائج اختبار الحسابين والجهازين، مع عدم اعتبار فحص TypeScript بديلاً عنها.

## 8. الحكم النهائي

**التطبيق قابل للاستمرار في التطوير، لكنه غير جاهز لإعلان parity أو نجاح مالي/أمني كامل.**  
أولوية العمل ليست إضافة fallback أو قاعدة بيانات ثانية، بل إثبات عقود Production ثم جعل الهاتف وAPI عملاء نحيفين لهذه العقود. أي إصلاح يتطلب تعديل Schema أو RLS أو RPC أو Trigger يجب أن يتوقف حتى يصرح المستخدم به صراحة.