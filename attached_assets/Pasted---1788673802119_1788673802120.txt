أكيد. هذه النسخة **الكاملة والنهائية للنسخ**، وأضفت فيها بشكل صريح أن التطبيق **حالياً غير قادر على إنشاء حجز بشكل طبيعي** وأن المطلوب ليس فقط إظهار الأيام والأوقات، بل الوصول إلى **حجز ناجح فعلياً بنفس مسار المنصة الأصلية**.

```text
هذه المهمة أولوية قصوى ويجب حلها نهائياً، وليس عمل workaround أو تعديل واجهة فقط.

==================================================
المشكلة الحالية — CRITICAL
==================================================

تطبيق الهاتف حالياً غير قادر على إنشاء حجز جلسة للطالب بشكل طبيعي.

المشكلة تظهر في شاشة إنشاء الحجز:

- المعلم يظهر.
- المادة تظهر.
- الرصيد يظهر.
- يمكن الوصول إلى شاشة اختيار اليوم والوقت.
- لكن أيام وساعات توفر المعلم لا تصل بشكل صحيح من المنصة الأصلية.
- تظهر رسالة مثل:

"لم تصل أيام توفر المعلم من المنصة. تأكد من توفر المعلم ومواعيده."

والنتيجة النهائية أن الطالب لا يستطيع إكمال إنشاء الحجز بشكل طبيعي.

وهذا غير مقبول.

المعلم لديه Availability ومواعيد فعلية في المنصة الأصلية، والمواعيد متاحة فعلياً، لكن تطبيق الهاتف لا يعكسها بشكل صحيح وبالتالي لا يستطيع الطالب إنشاء الحجز.

الصور المرفقة توضح المشكلة الحالية في تطبيق الهاتف.

أريد حل ROOT CAUSE نهائي، وليس إصلاحاً شكلياً.

==================================================
الهدف النهائي الإلزامي
==================================================

أريد أن يصل تدفق الطالب إلى:

Teacher
→ Subject
→ Real Availability
→ Available Days
→ Available Times
→ Valid Slot
→ Booking Request
→ API
→ Successful Booking

بنفس منطق المنصة الأصلية.

لا أعتبر المهمة مكتملة بمجرد أن تظهر الأيام والأوقات في الواجهة.

المطلوب أن يستطيع الطالب فعلياً إنشاء الحجز بشكل طبيعي.

==================================================
ممنوعات مهمة جداً
==================================================

ممنوع تماماً:

- fallback محلي.
- mock availability.
- hardcoded days.
- hardcoded times.
- default availability.
- افتراض الساعة 17:00.
- افتراض أن المعلم متاح.
- إنشاء Availability من الهاتف.
- اختراع بيانات غير موجودة في المنصة.
- تغيير رسالة الخطأ فقط.
- إخفاء رسالة الخطأ بدون إصلاح السبب.
- تعديل UI فقط.
- استخدام بيانات تجريبية لإظهار أن الشاشة تعمل.
- اعتبار TypeScript PASS دليلاً على نجاح الحجز.
- اعتبار Android/iOS build PASS دليلاً على نجاح الحجز.
- إنشاء API جديد لا علاقة له بمصدر المنصة لمجرد جعل الشاشة تعمل.
- تغيير قواعد الحجز الأصلية بدون دليل.

أي بيانات تظهر في التطبيق يجب أن تأتي من مصدر البيانات الحقيقي المستخدم في المنصة الأصلية.

==================================================
المرحلة 1 — READ ONLY ROOT CAUSE INVESTIGATION
==================================================

ممنوع تعديل الكود في البداية.

ابدأ بتحقيق كامل READ-ONLY.

تتبع عملية الحجز من البداية للنهاية في:

1. المنصة الأصلية.
2. API.
3. Supabase.
4. تطبيق الهاتف.
5. Git/GitHub/history.

لا تغير أي ملف قبل معرفة السبب الحقيقي.

==================================================
المرحلة 2 — اكتشف مصدر Availability في المنصة الأصلية
==================================================

أريد معرفة المصدر الحقيقي الذي تعتمد عليه المنصة الأصلية لتحديد توفر المعلم.

حدد بالضبط:

- الجدول.
- view إن وجد.
- RPC إن وجد.
- API endpoint.
- service.
- hook.
- query.
- relation.
- transformer.
- mapper.

ابحث في المشروع بالكامل عن:

availability
available_days
available_from
available_to
teacher availability
teacher schedule
schedule
day_of_week
start_time
end_time
timezone
teacher_subjects
public_profiles
subjects
bookings

لا تعتمد على أسماء متوقعة.

اقرأ implementation الفعلي.

==================================================
المرحلة 3 — تتبع المعلم والمادة والـIDs
==================================================

تحقق من العلاقة الفعلية بين:

teacher
public_profiles
teacher_subjects
subjects
availability
bookings

حدد:

teacher ID
profile ID
subject ID
availability owner ID

ثم تحقق:

هل تطبيق الهاتف يستخدم نفس IDs التي تستخدمها المنصة الأصلية؟

إذا كان الهاتف يستخدم ID مختلفاً، فهذا قد يكون ROOT CAUSE.

لا تنشئ mapping جديداً من عندك.

استخدم نفس mapping الموجود في المنصة الأصلية.

==================================================
المرحلة 4 — قارن المنصة الأصلية بتطبيق الهاتف
==================================================

أنشئ مقارنة فعلية:

| الوظيفة | المنصة الأصلية | تطبيق الهاتف | الحالة |
|---|---|---|---|
| جلب المعلم | | | |
| جلب المادة | | | |
| teacher ID | | | |
| subject ID | | | |
| Availability source | | | |
| available_days | | | |
| available_from | | | |
| available_to | | | |
| day mapping | | | |
| time mapping | | | |
| timezone | | | |
| existing bookings | | | |
| conflict checking | | | |
| duration | | | |
| booking validation | | | |
| booking endpoint | | | |
| booking request | | | |
| booking response | | | |

لا تكتب MATCH إلا إذا كان مطابقاً فعلياً في الكود.

==================================================
المرحلة 5 — اكتشف لماذا الأيام لا تصل
==================================================

حدد السبب الحقيقي.

أريد دليلاً على السبب، وليس تخميناً.

تحقق من كل الاحتمالات:

- endpoint خاطئ.
- query خاطئة.
- table خاطئ.
- relation خاطئة.
- teacher ID خاطئ.
- subject ID خاطئ.
- availability owner ID خاطئ.
- field name خاطئ.
- API لا يعيد availability.
- API يعيد availability والهاتف يهملها.
- mapping خاطئ.
- response parsing خاطئ.
- state لا يتم تحديثه.
- filter يحذف الأيام.
- filter يحذف الساعات.
- days format مختلف.
- day indexing مختلف.
- Arabic/English day mapping.
- Sunday/Monday convention مختلف.
- timezone conversion خاطئ.
- UTC/local conversion خاطئ.
- availability تعتمد على teacher_subjects.
- availability مرتبطة بالمادة.
- availability موجودة في جدول مختلف.
- availability موجودة لكن RLS تمنع الوصول.
- الطلب يرسل teacher_id غير الصحيح.
- البيانات تصل ولكن يتم تحويلها إلى [].
- البيانات تصل لكن validation ترفضها.
- أي سبب آخر يثبته الكود.

لا تقل "ربما".

حدد:

ROOT CAUSE = ______

والدليل:

SOURCE = ______

FILE = ______

FUNCTION = ______

==================================================
المرحلة 6 — افحص Git/GitHub والتاريخ
==================================================

هذه نقطة مهمة جداً.

المشروع أو المنصة كان يعمل سابقاً.

ابحث في Git history عن النسخة التي كانت تعمل.

استخدم:

git log
git blame
git diff
git show

وابحث عن أي implementation سابق متعلق بـ:

availability
booking
teacher schedule
teacher availability
slots

قارن:

WORKING VERSION

ضد

CURRENT VERSION

إذا وجدت كوداً سابقاً كان يجلب Availability بشكل صحيح، لا تعيد اختراع الحل.

حدد ما الذي تغير وأدى إلى المشكلة الحالية.

==================================================
المرحلة 7 — افحص API الحقيقي
==================================================

حدد API الذي تعتمد عليه المنصة الأصلية لجلب بيانات المعلم وAvailability.

إذا كان هناك endpoint أصلي:

اجعل الهاتف يستخدم نفس العقد.

إذا كان API الحالي يعيد المعلم بدون Availability بينما المنصة الأصلية تحصل عليها من endpoint آخر:

أصلح المصدر.

لا تستخدم fallback محلي.

إذا كان هناك API صحيح بالفعل ولكن Mobile mapping خاطئ:

أصلح Mobile mapping.

إذا كان API يحتاج تعديل:

عدله فقط إذا كان ذلك لا يتطلب تغيير Production contract.

==================================================
المرحلة 8 — Availability يجب أن تكون Source of Truth
==================================================

قاعدة المشروع:

SOURCE OF TRUTH = المنصة الأصلية / Supabase / API الفعلي.

الهاتف لا يقرر Availability.

الهاتف يعرض Availability القادمة من المصدر.

ممنوع:

const availableDays = [...]
const start = 17
const end = 22

وممنوع:

defaultDays
fallbackDays
mockDays
fakeAvailability
defaultAvailability

إذا Availability غير موجودة فعلياً:
اعرض عدم التوفر.

إذا Availability موجودة:
يجب أن تظهر.

==================================================
المرحلة 9 — الأيام
==================================================

اكتشف convention الحقيقي المستخدم في المنصة.

هل:

0 = Sunday

أم:

0 = Monday

أم:

"sun"
"mon"

أم أسماء أخرى؟

لا تفترض.

استخدم نفس implementation الموجود في المنصة الأصلية.

==================================================
المرحلة 10 — الوقت
==================================================

حدد:

available_from
available_to

وحدد كيف يتم تحويلهما إلى slots.

إذا:

available_from = 16:00
available_to = 22:00
duration = 60

فلا يجوز عرض بداية تؤدي إلى انتهاء الجلسة بعد 22:00.

لكن لا تفترض duration = 60.

استخرج duration من المنصة الأصلية.

استخدم نفس خوارزمية slot generation إذا كانت موجودة.

==================================================
المرحلة 11 — TIMEZONE
==================================================

تحقق بدقة من:

- timezone المعلم.
- timezone الطالب.
- timezone الخادم.
- timezone قاعدة البيانات.
- هل availability local time أم UTC.
- هل booking time يخزن UTC.
- أين يحدث conversion.

يجب أن يكون الوقت الظاهر في الهاتف مطابقاً للوقت الظاهر في المنصة الأصلية.

لا تصلح timezone بالتخمين.

==================================================
المرحلة 12 — EXISTING BOOKINGS
==================================================

Availability وحدها لا تكفي.

يجب معرفة كيف تمنع المنصة الحجز في وقت محجوز بالفعل.

مثلاً:

Availability:
16:00 → 22:00

لكن:

18:00 → 19:00 محجوزة.

يجب أن يتبع الهاتف نفس منطق المنصة.

حدد:

- كيف تقرأ المنصة bookings.
- كيف تتحقق من التعارض.
- هل تستخدم RPC.
- هل تستخدم API.
- كيف تتعامل مع overlapping sessions.

لا تخترع conflict algorithm إذا كانت المنصة لديها واحدة.

==================================================
المرحلة 13 — راجع إنشاء الحجز نفسه
==================================================

هذه نقطة CRITICAL.

حتى لو أصلحت Availability، لا تعتبر المهمة مكتملة.

تتبع:

زر "تأكيد الحجز"

حتى:

booking request

ثم:

API

ثم:

Supabase

ثم:

booking response

حدد:

- endpoint.
- HTTP method.
- request body.
- headers.
- Authorization.
- teacher_id.
- subject_id.
- date.
- start time.
- end time.
- duration.
- subscription.
- remaining_minutes.
- أي حقول أخرى.

قارنها حرفياً مع المنصة الأصلية.

==================================================
المرحلة 14 — التطبيق حالياً غير قادر على إنشاء الحجز
==================================================

تعامل مع هذا كـBLOCKER حقيقي.

المهمة ليست:

"إظهار Availability"

فقط.

المهمة هي:

"تمكين الطالب من إنشاء حجز جلسة بشكل طبيعي من البداية للنهاية."

لذلك يجب اختبار:

Student
→ Teacher
→ Subject
→ Day
→ Time
→ Duration
→ Confirm Booking
→ API
→ Booking Created

إذا توقفت العملية عند أي نقطة:

حدد بالضبط أين توقفت ولماذا.

==================================================
المرحلة 15 — إصلاح ROOT CAUSE
==================================================

بعد إثبات السبب:

نفذ أقل تعديل ممكن.

الأولوية:

1. إصلاح مصدر Availability.
2. إصلاح IDs.
3. إصلاح API contract.
4. إصلاح mapping.
5. إصلاح timezone.
6. إصلاح slot generation.
7. إصلاح conflict validation.
8. إصلاح booking request.
9. إصلاح UI فقط إذا كان ضرورياً.

لا تعيد كتابة شاشة الحجز بالكامل إذا كان السبب في API.

==================================================
المرحلة 16 — End-to-End Testing
==================================================

بعد الإصلاح يجب تنفيذ اختبارات فعلية.

TEST 1
المعلم متاح فعلياً في المنصة.

Expected:
أيام Availability تظهر في الهاتف.

TEST 2
اختيار يوم متاح.

Expected:
الساعات تظهر.

TEST 3
اختيار ساعة داخل Availability.

Expected:
الساعة تصبح قابلة للاختيار.

TEST 4
اختيار ساعة خارج Availability.

Expected:
مرفوضة.

TEST 5
اختيار وقت ينتهي بعد available_to.

Expected:
مرفوض.

TEST 6
وقت محجوز مسبقاً.

Expected:
يتبع نفس سلوك المنصة الأصلية.

TEST 7
ضغط "تأكيد الحجز".

Expected:
إرسال request صحيح.

TEST 8
API يستقبل الطلب.

Expected:
لا يوجد 400/401/403/404/409 غير متوقع.

TEST 9
الحجز يتم إنشاؤه.

Expected:
Booking Created.

TEST 10
بعد إنشاء الحجز:

Expected:
يظهر الحجز في التطبيق بنفس البيانات.

TEST 11
فتح المنصة الأصلية.

Expected:
الحجز يظهر هناك أيضاً إذا كان المصدر مشتركاً.

TEST 12
تسجيل خروج ثم دخول.

Expected:
الحجز ما زال موجوداً.

TEST 13
Teacher غير متاح.

Expected:
لا يمكن إنشاء حجز.

TEST 14
بدون Authorization.

Expected:
401.

==================================================
المرحلة 17 — لا تعتبر BUILD نجاحاً
==================================================

هذه نقطة إلزامية.

لا تقل:

TypeScript PASS
Android PASS
iOS PASS

إذن المشكلة انتهت.

هذا غير صحيح.

Build PASS يعني أن الكود يبنى فقط.

نجاح المهمة يجب أن يعتمد على:

REAL AVAILABILITY RESPONSE

ثم:

REAL SLOT GENERATION

ثم:

REAL BOOKING REQUEST

ثم:

REAL BOOKING SUCCESS

==================================================
المرحلة 18 — Production Safety
==================================================

لا تعدل تلقائياً:

- Production Database
- Schema
- RLS
- RPC
- Functions
- Triggers
- Auth
- VPS
- Stripe
- بيانات المستخدمين

إذا اكتشفت أن الحل يحتاج Production:

توقف قبل التعديل واكتب:

PRODUCTION BLOCKER

ثم اشرح:

1. ما الذي ينقص؟
2. لماذا لا يمكن حل المشكلة من Mobile/API؟
3. ما التغيير المطلوب؟
4. ما الجدول/RPC/RLS المسؤول؟
5. ما مخاطر التغيير؟
6. هل يوجد حل آمن بدون Production؟

لا تعدل Production من نفسك.

==================================================
المرحلة 19 — التقرير النهائي
==================================================

أنشئ ملف:

BOOKING_AVAILABILITY_ROOT_CAUSE_REPORT.md

ويجب أن يحتوي:

1. EXECUTIVE SUMMARY
2. CURRENT PROBLEM
3. ROOT CAUSE
4. SOURCE OF TRUTH IN ORIGINAL PLATFORM
5. SOURCE OF TRUTH IN MOBILE
6. EXACT MISMATCH
7. GIT HISTORY FINDINGS
8. DATABASE TABLES
9. RELATIONSHIPS
10. TEACHER ID FLOW
11. SUBJECT ID FLOW
12. AVAILABILITY FLOW
13. DAY MAPPING
14. TIME MAPPING
15. TIMEZONE
16. EXISTING BOOKINGS
17. SLOT GENERATION
18. BOOKING REQUEST CONTRACT
19. BOOKING RESPONSE
20. FILES CHANGED
21. EXACT FIX
22. BEFORE
23. AFTER
24. END-TO-END TEST RESULTS
25. REMAINING BLOCKERS
26. PRODUCTION CHANGES = YES/NO

==================================================
النتيجة التي أريدها
==================================================

لا أريد منك أن تقول:

"تم إصلاح شاشة الحجز."

أريد أن تصل للنتيجة:

"المعلم الموجود في المنصة الأصلية لديه Availability فعلية من المصدر ______.

تطبيق الهاتف كان يقرأ Availability من ______.

الاختلاف كان ______.

لذلك كانت الأيام تصل فارغة/لا تصل.

تم إصلاح المصدر/العقد/mapping.

الآن الهاتف يستقبل Availability الحقيقية:

days = ______
from = ______
to = ______

وتم توليد slots بنفس منطق المنصة.

ثم تم إرسال Booking Request الحقيقي:

endpoint = ______

والنتيجة:

Booking Created = SUCCESS

والحجز يظهر في التطبيق والمنصة الأصلية."

==================================================
قاعدة نهائية
==================================================

لا تعتبر المهمة مكتملة إلا إذا ثبت الآتي:

1. المعلم المتاح في المنصة الأصلية يظهر كمتاح في الهاتف.
2. أيام توفره تظهر بشكل صحيح.
3. ساعات توفره تظهر بشكل صحيح.
4. الـtimezone مطابق.
5. الأوقات المحجوزة يتم التعامل معها مثل المنصة.
6. الطالب يستطيع اختيار موعد صالح.
7. الطالب يستطيع الضغط على "تأكيد الحجز".
8. الطلب يصل إلى الـAPI الصحيح.
9. الحجز يتم إنشاؤه بنجاح.
10. الحجز يظهر بعد الإنشاء.
11. تسجيل الخروج والدخول لا يفقد الحجز.
12. لا يوجد fallback أو mock أو hardcoded availability.

ابدأ الآن.

أولاً READ-ONLY ROOT CAUSE INVESTIGATION.

لا تعدل الكود قبل إثبات المصدر الحقيقي للـAvailability في المنصة الأصلية.

بعد إثبات السبب، أصلح السبب الحقيقي فقط.

ثم نفذ End-to-End verification حتى إنشاء الحجز فعلياً.

إذا لم تستطع إكمال الحجز بسبب Production dependency، لا تدّعي النجاح؛ اكتب PRODUCTION BLOCKER مع الدليل الدقيق.

هذه المهمة أهم من أي تحسين UI آخر، ولا تنتقل إلى مرحلة أخرى قبل حلها بالكامل.
```
